# AGENTS.md

## 优先级

- 用户本轮明确的目标、阶段、范围和时间预算优先。不要把最终验收要求前置成开发步骤。
- 只做当前阶段的最短闭环。发现阶段外问题先记录；扩大范围前必须说明。

## 通用边界

- 默认在当前分支和工作树原地最小修改，保留无关 dirty。未经同意不建 worktree/Library，不做全库 Refresh、重转或重序列化。
- 用户在电脑前时，占用 Unity、鼠标、键盘或窗口焦点前先说明。危险、不可恢复或生产操作必须取得本轮授权。
- 人工接管的 Prefab 是视觉事实源；已有 Prefab 只增量修改，禁止转换器或 Creator 覆盖。
- 老客户端同状态运行结果是最终验收标准。静态、编译、运行和视觉验收是不同完成等级，如实报告。
- 专项规则按任务从 [Docs/README.md](Docs/README.md) 选择读取，不常驻加载。旧规则全文仅作检索档案，见 [.agents/rules/legacy-project-rules-20260812.md](.agents/rules/legacy-project-rules-20260812.md)。

## UI 模式

- “UI 对接、先跑通、补功能、接逻辑/协议、快速完善按钮”使用 `ui-function-fast-pass`：补最小功能组件、逻辑、协议和刷新；编译后停止。
- “UI 精修、视觉一致、布局/模型/特效修复”使用 `fix-view`：只修已有 Prefab 的可见表现。
- “巡检、验收、最终收口、逐叶测试、真实 Web 对比”使用 `audit-game-ui-route`：只做最终验收与缺陷报告。
- 明确要求首次转换且目标没有可编辑 Prefab 时，才使用 `convert-module`。
- 同一轮默认只启用一种主模式。切换模式须由用户明确要求，或当前模式结束后获得确认；不得把多个 Skill 的完成标准取并集。

## 时间止损

- 小型单 Tab 参考预算：盘点 20 分钟、功能实现 30 分钟、编译 5 分钟。这是偏航提醒，不是质量承诺。
- 连续 10 分钟没有产生当前阶段的直接产物，或同一环境问题重复两次，立即停止该方向并报告。
- “缺功能组件”只补能显示状态并承载点击的最小节点；完整布局、装饰、模型和动效留给视觉阶段。

## 提交与文档

- 只暂存本任务文件，不夹带其他 dirty。未经用户要求，不因阶段性编译通过自动提交。
- 只有架构、公共工具/组件、协议主链、构建发布方式或可复用疑难结论变化时更新正式文档；普通页面实现不自动扩写规则。
