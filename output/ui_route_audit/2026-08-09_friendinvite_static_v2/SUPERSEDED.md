# Superseded

此目录保留修正后的 83 节点拓扑，但正式账的路线名误写为版本化身份 `mainui.friend-invite-v2`。schema 6 禁止原地修改路线身份或 manifest。

最终拓扑已在 `2026-08-09_friendinvite_static_v3` 以稳定逻辑路线 `mainui.friend-invite` 全新初始化；v2 不再作为权威结果。
