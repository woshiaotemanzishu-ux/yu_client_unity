interface:
  display_name: "游戏界面运行态巡检"
  short_description: "深度优先点完 UI 功能树，核对状态、版本、性能并复验"
  default_prompt: "Use $audit-game-ui-route to list one UI route tree, finish one branch to its leaf, fix every verified gap, and re-run the full state sequence."
