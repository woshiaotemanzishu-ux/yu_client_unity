interface:
  display_name: "游戏界面运行态巡检"
  short_description: "用老端与 Unity Web 逐路由点击、取证、修复并复验界面"
  default_prompt: "Use $audit-game-ui-route to audit one game UI route against the old client, fix verified gaps, and re-run the same clicks."
