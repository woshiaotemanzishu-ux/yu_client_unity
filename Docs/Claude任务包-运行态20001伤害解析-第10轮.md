# Claude 任务包 - 运行态 20001 伤害解析 - 第10轮

执行仓库: `D:\GitProject\yu_client_unity`

老端仓库: `D:\GitProject\yu_client`

目标: 完全复刻老 Laya 客户端。老端结论必须来自运行时 `http://127.0.0.1:8090/index.html`；运行态暂时抓不到时，必须明确写出卡点，并用老端源码、真实配置和同服真连协议证据补足最低证据链，禁止把静态 `.scene` 当最终真相。

上一轮基线:

- 第9轮任务包: `Docs/Claude任务包-运行态真实战斗收尾-第9轮.md`
- 第9轮结果提交: `daf57d408`
- 第9轮报告: `Docs/RuntimeCompare/MainQuest-CombatFinish-第9轮.md`
- 第9轮已确认:
  - Unity 真连已实发 `20024 "c" 1` + `20001`，走 `NetManager`，非独立 socket。
  - 服务器返回真实 `20001` 攻击结果广播 `108B`，原始字节能匹配我方 `role_id=4294967355`、`skill_id=59100001`、`attack_pos=(5374,2672)`、`defender=1129`。
  - 真实首杀技能 `59100001` 是圆形 AOE: `mod=2`、`range=1`、`area=350`、`num=[1,4]`；Unity 已实现圆形 AOE 目标收集，但真连只验证了主目标子集。
  - 本端 `12009/12006 -> SceneManager -> MonsterRenderer` 血条/销毁链已就绪；但服务器本次攻击伤害不走 `12009`，而走 `20001 S2C FightVo.defense_list`。
  - 当前真实 blocker: 未解析 `20001 S2C defense_list`，所以不能真实扣血、死亡、推进 `100030` 击杀进度。

## 必读

1. `AGENTS.md`
2. `.github/copilot-instructions.md`
3. `Docs/Shenxiao编码规范.md`
4. `Docs/Shenxiao重构实施方案.md`
5. `Docs/LayaUI转换流水线.md`
6. `Docs/Shenxiao进游戏链路.md`
7. `Docs/RuntimeCompare/MainQuest-CombatFinish-第9轮.md`
8. `Docs/Claude任务包-运行态真实战斗收尾-第9轮.md`
9. 当前 Unity 代码:
   - `Assets/Scripts/Module/Core/Scene/FightController.cs`
   - `Assets/Scripts/Module/Core/Scene/SceneCombat.cs`
   - `Assets/Scripts/Module/Core/Scene/SceneController.cs`
   - `Assets/Scripts/Module/Core/Scene/SceneManager.cs`
   - `Assets/Scripts/Module/Core/Scene/MonsterRenderer.cs`
   - `Assets/Scripts/Module/Core/Scene/Vo/MonsterVo.cs`
   - `Assets/Scripts/Module/Core/Skill/SkillConfigs.cs`
10. 老端运行时 `http://127.0.0.1:8090/index.html`
11. 老端源码中至少要查清:
   - `h5/src/scene/fight/FightController.ts`
   - `h5/src/scene/Scene.ts`
   - `h5/src/scene/SceneController.ts`
   - 所有 `handler20001`、`FightVo`、`DefenseVo`、`RefreshObjVo`、`ForceDoDead`、`ShowFont`、`damage_flag`、`defense_list` 相关定义和调用。

## 本轮目标

把第9轮卡住的真实战斗结果闭环推进一步:

`可见怪 -> 技能释放 -> 真实 20024/20001 -> 服务器 20001 S2C 攻击结果广播 -> 解析 defense_list -> 用服务器真实 hp/damage 驱动血条扣减/死亡移除 -> 观察 100030 击杀进度是否真实推进`

核心只做 `20001 S2C` 解析与真实数据驱动。不要扩散到完整自动战斗 AI、完整动作帧、完整特效、飘字系统、掉落、伙伴/神祇、活动副本。

## 硬性边界

- 不允许本地伪造伤害、扣血、死亡、掉落或任务进度。
- 不允许为了截图直接改 `MonsterVo.Hp`；只有解析到服务器真实新 hp 或死亡字段后才能喂给现有场景链。
- 不允许猜 `20001 S2C` 字段。每个字段必须来自老端源码、真实 108B 样本、或新的真连转储验证。
- 不允许把 `12009` 当本轮主伤害路径；第9轮已确认本次攻击伤害走 `20001 S2C`。`12009/12006` 只能作为已有通用链路或备用广播路径记录。
- 不允许 hardcode `1129`、`1134`、`59100001`、`5374/2672` 作为最终业务逻辑；调试 harness 可以取证，提交前不得落入业务代码。
- 不允许绕过 `NetManager`。
- 不允许提交 `.playwright-cli/`、`output/`、字体 SDF、Generated/Bind。
- 不允许手改生成 Bind，不允许 `transform.Find` 取业务节点。
- 如果老端运行态仍卡加载页，必须在报告写清；但仍需尝试运行态取证，不允许完全跳过。

## P0: 保护第9轮基线

先复核并保护:

- `dotnet build yu_client_unity.slnx -v:minimal` 必须通过。
- `FightController` 注册、`20024/20001` 发包、圆形 AOE 收集不能退化。
- 真实 `MonsterVo -> MonsterRenderer -> GameView` 可见怪不能退化。
- `AppConfig.asset` 如需临时打开冒烟账号/自动登录，提交前必须还原。
- 工作树只允许本轮相关代码、报告、任务包改动；`.playwright-cli/`、`output/` 不入库。

## P1: 老端 20001 S2C / FightVo 解析证据

必须从老端运行态和源码确认:

- `handler20001` 如何读取攻击者头、技能、坐标、角度。
- `defense_list` 的数量字段、每个防御者的 id/type/hp/maxHp/damage/damage_flag/死亡/位置/状态字段顺序。
- 老端在哪里用 `defense_list` 调 `RefreshObjVo`、扣 hp、`hp==0`、`ForceDoDead`、飘伤害。
- `12009` 与 `20001 S2C` 的关系: 哪些场景走 `12009`，哪些战斗伤害走 `20001 S2C`。
- 第9轮 108B 原始字节样本中能确定哪些字段，不能确定的字段必须继续抓样本或记录 blocker。

产物:

- `Docs/RuntimeCompare/MainQuest-FightVoDamage-第10轮.md`
- 报告必须包含老端运行态尝试结果、源码逐字段证据、108B 样本解析表、仍未确认字段清单。

## P2: Unity 最小 `20001 S2C` 解析

只在 P1 字段证据足够时实现:

- 在 `FightController.On20001Broadcast` 或等价位置解析真实 `20001 S2C`。
- 解析结果必须保留原始字节长度、攻击者、技能、攻击点、防御者列表、服务器给出的 hp/damage/死亡信息。
- 解析出的防御者必须映射到真实 `SceneManager` 中的 `MonsterVo`/角色对象；找不到对象只能记录 warning，不造假对象。
- 只把服务器真实 hp/damage/死亡喂给已有 `SceneManager`/`MonsterRenderer` 链路；不要开新假血条路径。
- 如果字段证据不足，代码只保留安全转储/日志，不得半猜半写入 hp。

验收:

- 编辑期 108B 样本解析 harness 能把第9轮样本解析到与报告一致。
- Play 态真连收到 `20001 S2C` 后，日志能列出 defense_list。

## P3: 真实扣血/死亡最小闭环

只有 P2 能解析到服务器真实新 hp 或死亡字段后才推进:

- Play 态点击/驱动真实技能，收到 `20001 S2C`。
- `MonsterRenderer` 血条按服务器数据真实变化。
- 如果 hp 到 0 或死亡字段到达，怪物可见模型/名牌/血条真实移除或进入死亡状态。
- 如果单次攻击不足以杀怪，只允许重复真实攻击请求，直到服务器数据变化；不能本地减血。
- 如果服务端返回的 damage/hp 不足以改变可见状态，报告记录真实原因。

## P4: 主线任务 `100030` 击杀进度

在 P3 后验证:

- `100030` 降服 3 只是否因真实死亡/服务器任务更新推进。
- 如果有 `30000/30001` 或其他任务推送，记录协议证据。
- 如果击杀可见但任务不推进，记录真实 blocker，不本地改任务。

## P5: 只记录不扩散

只记录，不编码:

- 完整自动战斗 AI、挂机循环、寻怪循环。
- 完整技能 CD、动作帧、fight-movie 精准时序、特效、音效、伤害飘字。
- 直线/扇形 AOE 几何、PvP 目标、伙伴/神祇/天赋加成。
- 掉落、怪物 AI、活动副本、BOSS、队伍协同。

## 验收与提交

1. `dotnet build yu_client_unity.slnx -v:minimal`
2. 真实运行态截图/日志/协议证据写入 `Docs/RuntimeCompare/MainQuest-FightVoDamage-第10轮.md`
3. 如果解析 `20001 S2C`，必须有样本解析表和真连日志；如果未解析，必须写清阻塞字段和下一步取证方式。
4. 只提交本轮任务包、代码、报告，不带 `.playwright-cli/`、`output/`、字体 SDF、Generated/Bind。
5. 最终总结必须说明:
   - 老端 `20001 S2C FightVo.defense_list` 是否逐字段确认。
   - Unity 是否实现真实解析。
   - 是否出现服务器真实 hp/damage/death 数据。
   - 可见血条/怪物销毁是否真实发生。
   - `100030` 任务进度是否真实推进。
   - 仍有真实 blocker 和下一轮建议。
