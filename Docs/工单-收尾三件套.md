# 工单:收尾三件套(灵魄强化 16702 + 神装合成 15020 + 排位赛壳 28001/02/03)

日期:2026-07-02(第20轮侦察代理产出,主控审定)
意义:此三件完成后,**主线链 543 任务的全部 27 种 ctype 客户端侧全部处理完毕**(复核:链自 100010 至 104560 无分叉无遗漏)。

## 1. 灵魄强化 16702(解 101525 ctype50「御魂总等级达3」)——判定无缺口
- 请求:发 "l"(goods_id,已穿戴符文实例);回包:code:i, rune_point:i, goods_id:l。
- 消耗 rune_point(符文经验,镶嵌溢出自然产生);经验不足=err167 显码。
- 成功后服务端自动触发 rune_lv 事件驱动 ctype49/50;判定=实时统计穿戴符文 level 之和,无缺口。
- Unity 增量:RuneController 注册 16702 + Upgrade(goodsId);RuneWearShellView 已镶嵌槽位加[强化]按钮;Model 更新 RunePoint。

## 2. 神装合成 15020(解 101725 ctype73「合成一次神装」)——判定无缺口
- 「神装合成」=通用装备合成(COMPOSE_EQUIP type=2),无专门协议;服务端合成成功且规则 type==2 → red_equip_combine +1。
- 请求(对标 CompositeController.ts:107 WriteBegin(15020)):i rule_id + h count + 逐 l goods_id(固定材料)+ h count + 逐 l goods_id(指定材料)。
- 回包:code:i, compose_type:c, rule_id:i, goods_id:l(code==1 成功)。
- 配表 config_goods_compose(合成规则,type==2 为装备类;Erlang term 字符串用 ErlangParser)——需加 SYNC_LIST+复制。
- Unity 增量:ComposeController(15020+Compose(ruleId, regulars, specifies) 动态 fmt)+TEMP 壳(列 config 中 type=2 的规则前 N 条:
  名称/所需材料 goods 名,材料从 BagModel 匹配够则[合成]按钮可点——匹配逻辑最小化:只按 regular_mat 的 type_id 在背包找 goods_id)。

## 3. 排位赛壳 28001/28002/28003(101465 ctype35)——⚠服务端断链
- **服务端缺口 #2**:判定读 mod_daily ?JJC_USE_NUM,唯一 increment 在 mod_jjc_cast.erl:87 被注释——挑战流程完整但计数永不增长,
  任务无法自然完成。**需人工恢复该行或改判定读源**(与大妖 63 并列服务端工单)。
- 客户端先备壳(服务端修复后即可用):
  - 28001 页面信息(无参;回包 rank,history_rank,reward_rank,combat,hp,num,num_refresh,honour,is_reward,pet_id,break_id_list——
    实现时从 ClientProtocol.json:2729 摘原文逐字段读)
  - 28002 随机对手(无参;回包 role_list[{rank,role_id,combat,hp,pet_id,figure…}]——figure 块读法参考既有 12003/14200 处理或按序读完)
  - 28003 挑战(发 "ilic" selfRank,rivalId,rivalRank,challengeType=0;回包 role_list,result,reward_list,break_reward_list)
  - TEMP 壳:显示我的排名/剩余次数 + [随机对手] + 对手列表首个[挑战]。
- ⚠28002/28003 的 figure 嵌套块较复杂——实现代理先摘 ClientProtocol 原文,读不全宁可按 Remaining 日志截断+诚实标注,不得错位后续字段。

## 4. LV(27)决策:维持降级 toast(UpAlertView=独立活动导览系统,不影响任务判定,不做)。

## 接线(主控):Proto 常量(16702/15020/28001/28002/28003)、Hub、DoTask ctype35→排位壳、RenderAll。
## 服务端工单(用户人工):①大妖63 guard(lib_special_boss_mod.erl:516);②排位35 increment(mod_jjc_cast.erl:87)。
