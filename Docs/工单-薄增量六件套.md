# 工单:薄增量六件套(翼影/圣器/神兵/宝石/挂机/橙装/熔炼)

日期:2026-07-02(第19轮侦察代理产出,主控审定)
特点:7 个任务全是「状态快照自动判定」(除挂机91为事件计数)——只要动作协议打通,任务自动完成,零任务专用代码。

## 1. OutWard 通用升星 16005(解 100665 翼影 ctype24 / 101045 圣器 ctype92 / 101345 神兵 ctype41)
- 服务端 guard:type_id∈{1,2}→16023;其它(3翼影/4圣器/5神兵)→**16005**(发 "c" type_id,无 autoBuy/goldType)。
- 回包 schema=16023 **去掉 etime/auto_buy**:errcode:i, type_id:c, stage:c, star:h, blessing:i, blessing_plus:i, ratio_list[u16×{rate:c, rate_num:h}]。
- Unity 增量:Proto.OUTWARD_STAR_UP_GENERIC=16005;OutWardController 注册+On16005(复制 On16023 少读2字段)+StarUpGeneric(typeId);
  OutWardModel.Apply16005(≈Apply16023 去2字段);OutWardShellView 加 3/4/5 三行(名字:翼影/古法符相/殒锋天刃)+升星按钮(StarUpGeneric);
  GameStart 对 3/4/5 也发 16002(16028 等级线与这三个任务无关,不用扩)。
- 老端枚举注释警示:Artifact=4=古法符相线,HolyDevice=5=殒锋天刃线(曾错位,以 mount.hrl ARTIFACT_ID=4/HOLYORGAN_ID=5 为准)。

## 2. 宝石镶嵌 15208/15209(解 101175 ctype48「宝石强化总等级达1」)
- 15208 镶嵌:发(equipPos:c, stonePos:c, goodsId:l)→ fmt "ccl";回包 res:i, equip_type:c, pos:c, type_id:i。
- 15209 拆除:发 "cc";回包 res:i, equip_type:c, pos:c。
- 最小面:EquipStoneController(2 协议)+TEMP 壳(装备位1/宝石槽1 + 从背包选宝石物品镶嵌——背包物品经 BagModel 现有数据,type=14 宝石过滤)。

## 3. 挂机奖励 13216(解 101211 ctype91「领取1次挂机收益」——唯一事件计数型)
- 13216 领取:C2S 无参;回包 code:i + exp_list[…](按 ClientProtocol "13216" 原文读完,实现时摘)。
- 最小面:OnHookController(13216)+壳一个[领取挂机收益]按钮;信息协议 13211/13212/13214 留后。

## 4. 橙装穿戴 15201(解 101205 ctype93「穿3件3阶橙装」)
- 15201 穿戴:发 "l"(goodsId 实例id);回包 res:i, goods_id:l, old_goods_id:l, type_id:i, cell_pos:c。
- 最小面:EquipWearController 或并入 EquipStrenController(1 协议)+背包壳/ItemTips 加[穿戴]按钮(装备类物品)——
  建议:ItemTipsView 装备分支加「穿戴」按钮(goods 实例存在且 IsEquip)→ 发 15201;回包 res==1 Toast("穿戴成功")。
- 任务需要 3 件 3 阶橙色+装备——活服掉落/任务奖励获得,壳只负责动作。

## 5. 装备熔炼 15024/15025(解 101285 ctype18「熔炼等级」)
- 15024 查询:无参;回包 level:h, exp:i。
- 15025 熔炼:发 [GoodsList](fmt=h count + 逐项 l goods_id/i num,对照老端 OnDevourEquipment WriteBegin(15025) 写法——
  GoodsController.ts:1211-1222:WriteFMT("h",count)+循环 WriteFMT("l",goods_id)+WriteFMT("i",goods_num));
  回包 code:i + exp_list[u16×{add_exp:h, ratio:c}];随后服务端另推 15024。
- Unity 已有 BagSmeltView.cs 壳(useBtn 打日志)——把 useBtn 接上(选中物品列表若壳里没有,先熔炼固定选择逻辑:
  从 BagModel 找 sell/白装类?⚠不臆造:最小=useBtn 发 15025 空列表会失败——改为壳内列背包装备类物品前 N 件供勾选,或先只接 15024 查询+把发包函数备好由 ItemTips/后续接。实现代理侦察 BagSmeltView 现有结构后定,报告说明)。

## 6. ⚠大妖挑战 63(101200)——服务端预存缺口,本轮不实现
- lib_special_boss_mod.erl:516 计数 guard 只认 BossId∈{402,403},任务需要 3400001 → 正常击杀永远不计数。
- **需要人工改服务端**(改 guard 或补 3400001);客户端 46xxx 移植等服务端定夺后再排。DoTask ctype63 保持降级提示。

## 接线(主控):Proto 常量、事件、ControllerHub、DoTask(24/92/41→OutWard 壳;48/93→对应壳;91→挂机壳;18→BagSmeltView 或壳)、RenderAll。
## 红线:同前;15025 发包字节序照老端 WriteBegin 写法;不臆造 UI 数据。
