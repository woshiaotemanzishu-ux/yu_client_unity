# Claude 任务包 - 运行态 12005 门闩修复 - 第20轮

## 0. 背景与本轮边界

目标仓库: `D:\GitProject\yu_client_unity`

老 Laya 客户端结论仍必须来自运行时 `http://127.0.0.1:8090/index.html` 或老端源码/运行日志双证据,不能把静态 `.scene` 当最终真相。

第19轮 worker 已完成,结果提交 `db4b126f0`,报告为 `Docs/RuntimeCompare/MainQuest-ContinuousKill-第19轮.md`。第19轮真实证据:

- Pump harness 跑满约 `574.9s / 5400 frames`,`r18=True`。
- Fresh-login smoke 链完成 `HTTP -> WebSocket -> 10004`,包含一次自动重连。
- `AppConfig.asset` 验证后已还原为全 0。
- 关键 blocker: 10004 后没有进入场景快照,无 `12002`,因此 `Round15/Round18` 没有触发,`20001` C2S/S2C 计数为 0。

注意: 第19报告把 blocker 写成“12005 未实现”。这不是可直接采用的结论。本仓库已有 12005 代码与历史真连证据:

- `Assets/Scripts/Framework/Net/Proto.cs`: `SC_CHANGE_SCENE = 12005`
- `Assets/Scripts/Module/Core/Scene/SceneEntryFlow.cs`: 监听 `EVT_GAME_START` 后调用 `SceneController.Instance.RequestEnterScene(RoleModel.Instance)`
- `Assets/Scripts/Module/Core/Scene/SceneController.cs`: `RequestEnterScene()` 发送 `SendFmt(12005, "iicchh", role.DunId, role.SceneId, 0, 0, 0, 0)`,并注册/解析 `On12005`
- `Docs/RuntimeCompare/MainQuest-Combat-第6轮.md`: 已有 `request 12005 -> 12005 ok -> 12100 -> 12002`
- `Docs/RuntimeCompare/MainQuest-ComboActualRun-第16轮.md`: 已有第16轮 `12005 ok` 与真伤害链证据

因此第20轮禁止重复猜包或另造 12005。本轮要定位的是: 第19 fresh-login smoke/reconnect 路径中,为什么现有 `EVT_GAME_START -> SceneEntryFlow -> RequestEnterScene` 没有执行或没有留下 `request 12005` 证据。

## 1. 必读规则

开始前必须阅读:

- `AGENTS.md`
- `.github/copilot-instructions.md`
- `Docs/Shenxiao编码规范.md`
- `Docs/Shenxiao重构实施方案.md`
- `Docs/Shenxiao进游戏链路.md`

如涉及老端运行态对比,优先使用 `http://127.0.0.1:8090/index.html` 真实运行时与源码互证。不要用记忆猜协议。

不要提交:

- `.playwright-cli/`
- `output/`
- 字体 SDF
- `Assets/Scripts/Generated/Bind`
- 临时 `AppConfig.asset` 验证改动

## 2. P0 - 保护第19轮基线

必须先验证并记录:

- 当前基线至少包含 `db4b126f0`。
- `dotnet build yu_client_unity.slnx -v:minimal` 0 error。若环境构建不可用,记录实际错误,不得假报通过。
- `Assets/_App/Configs/AppConfig.asset` 默认值必须为:
  - `devAccount: unity_dev_001`
  - `autoLoginSmokeTest: 0`
  - `autoEnterFirstRoleSmokeTest: 0`
  - `enableRound15ComboTest: 0`
  - `enableRound18ContinuousKill: 0`
- `git status --short` 中不能有字体 SDF、Generated/Bind、临时 AppConfig 被纳入提交。
- 正伤害链基础不能退化: `SkillConfigs.GetComboNext`、`SceneCombat` engage+combo 补发、`FightVo`/`DefenseVo` 解析、`MonsterRenderer` 基础链路保持。

## 3. P1 - 复核第19报告并纠正 blocker 表述

必须打开并复核:

- `Docs/RuntimeCompare/MainQuest-ContinuousKill-第19轮.md`
- `Docs/RuntimeCompare/MainQuest-Combat-第6轮.md`
- `Docs/RuntimeCompare/MainQuest-ComboActualRun-第16轮.md`

本轮报告中必须明确区分:

- 已确认事实: 第19 fresh-login smoke 中没有 `12002`,也没有 `request 12005` 日志。
- 不可直接成立的表述: “12005 未实现”。现有代码和历史证据说明 12005 已实现过。
- 当前待证根因: 第19路径没有触发 `EVT_GAME_START`,或 `SceneEntryFlow` 未安装/未收到事件,或 `RoleModel.HasBaseInfo/SceneId/DunId` 门闩导致 `RequestEnterScene` 跳过,或 reconnect/NormalClosure 改变了触发顺序。

## 4. P2 - 定位 GAME_START 到 SceneEntryFlow 的断点

必须沿当前代码和运行日志定位:

- `LoginController.OnEnterGame` / 10004 成功后做了哪些启动请求。
- `GameEntryFlow` 如何等待 `13001/10201/30005/13088@300@1/10202@3` 并发 `EVT_GAME_START`。
- 第19日志中是否出现这些门闩:
  - `13001`
  - `10201`
  - `30005`
  - `13088@300@1`
  - `10202@3`
  - `EVT_GAME_START`
  - `SceneEntryFlow`
  - `skip 12005: role base info is not ready`
  - `request 12005`
- `SceneEntryFlow.Install()` 是否在当前 Editor/Play/RunCommand 路径执行。若 `[RuntimeInitializeOnLoadMethod]` 在某种 harness 中未重新安装,必须给出证据。
- `ControllerHub.InitAll()` 是否在当前路径初始化了 `SceneController` 的协议注册。

若缺日志,允许补最小诊断日志,但必须:

- 使用 `GameLog`
- 不改变业务语义
- 不写本地伪造场景对象或假 12002
- 验证后保留有价值日志或解释为何移除

## 5. P3 - 最小修复原则

只有定位到真实断点后才允许修复。

允许的修复方向示例:

- 如果 `EVT_GAME_START` 未触发: 修正 game-start 门闩,让 fresh-login smoke 与正常登录一样等待真实 `13001/10201/30005/13088@300@1/10202@3` 后触发。
- 如果 `SceneEntryFlow` 未安装: 修正启动/安装入口,保证正常运行路径和 Editor harness 一致安装,不要重复注册造成多发。
- 如果 `RoleModel` 场景字段未就绪: 修正真实 `13001` 解析/赋值或进入场景前的默认来源,不能写假 sceneId。
- 如果 reconnect 后状态被清空: 修正 reconnect 状态恢复,保证不会在已进入游戏路径丢掉 `GAME_START` 或场景入口。

禁止:

- 另写一个新的 12005 猜包。
- 在 `LoginController.OnEnterGame` 中绕过 `EVT_GAME_START` 硬发 12005,除非有老端源码和当前门闩证据证明这就是正确接管点。
- 伪造 `12002`、伪造 `MonsterVo`、伪造 hp/death/task push。
- 为了通过验收本地扣血、删除怪物或推进 100030。

## 6. P4 - 运行态实跑验收

修复后必须用 Unity 真链路或等价 `EditorApplication.update + NetManager.Pump` harness 实跑。临时 `AppConfig.asset` 取证后必须还原。

同一会话中必须尽量记录以下链路:

1. `10004` 成功。
2. `GAME_START` 或明确门闩完成日志。
3. `SceneEntryFlow` 收到事件。
4. `request 12005: dunId=... sceneId=...`
5. `12005 ok: sceneId=...`
6. `request 12100`
7. `12002` 到达。
8. `MonsterVo` 同步且可被 `SceneManager.AllMonsters` 取到。
9. `Round15/Round18` 自动触发。
10. 至少两轮 `59100001` engage C2S 与 `59100002` combo C2S,记录目标/x/y/angle/时间戳。
11. 服务器真实 `20001` S2C damage/hp/flag。

只有真实 `hp==0/death flag/12006 DeleteSceneObj` 或等价删除到达,才算死亡闭环。只有真实 `30000/30001/30004/30005` 任务推送,才算 `100030` 推进闭环。

## 7. P5 - 报告与提交要求

若完成修复与实跑,新增或更新:

- `Docs/RuntimeCompare/MainQuest-SceneEntryGate-第20轮.md`

报告必须包含:

- 第19 blocker 复核结论。
- `12005 已存在` 的代码证据与历史证据。
- 本轮定位到的真实断点。
- 修复文件清单与原因。
- 实跑日志摘录: 10004/GAME_START/12005/12002/MonsterVo/Round15/Round18/20001。
- 未闭合项清单,只写有证据的问题。

若仍无法闭合,也必须提交同一报告,状态写 `BLOCKED`,并给出完整 blocker 证据,不要用“请用户手动执行”作为完成。

## 8. 下一轮建议

如果第20轮能稳定拿到 `12002 -> MonsterVo -> Round18`,但死亡/任务推送仍未闭合,第21轮再聚焦连续 combo 直至 hp==0、12006/delete、100030 任务推进。

如果第20轮发现 `GAME_START` 门闩缺某个真实协议,第21轮应先补该协议 handler 或正确的 ready flag,不要越过门闩直接驱动战斗。
