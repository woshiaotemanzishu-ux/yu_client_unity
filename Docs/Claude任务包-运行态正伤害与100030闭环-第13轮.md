# Claude 任务包 - 运行态正伤害与100030闭环 - 第13轮

执行仓库: `D:\GitProject\yu_client_unity`

老端仓库: `D:\GitProject\yu_client`

目标: 完全复刻老 Laya 客户端。老端结论必须优先来自运行时 `http://127.0.0.1:8090/index.html`；运行态抓不到时，必须明确卡点，并用老端源码、真实配置、真连协议证据补足最低证据链。禁止把静态 `.scene` 当最终真相。

上一轮基线:

- 第12轮任务包: `Docs/Claude任务包-运行态服务端伤害根因-第12轮.md`
- 第12轮结果提交: `5ca52676d`
- 第12轮报告: `Docs/RuntimeCompare/MainQuest-DamageRootCause-第12轮.md`
- 第12轮已确认:
  - `damage=0` 根因已定位到冒烟角色养成不足 + 服务端权威:角色 `云霄42852` / roleId `4294967355` / 等级 2 / 战力 1040，`att=33`、`wreck=17`、`def=17`、`Hp=740/740`。
  - `Hp=740` 与第 9/10 轮 `20001 S2C` 攻击者头互证，打出 3 条 `damage=0` 的就是该角色。
  - Unity 与老端 C2S/解析/时序无已确认差异；本轮客户端零代码改动是正确边界。
  - 老端运行态已从加载页推进到 `LoginView`，走同一 GM 后端 `223.109.142.26:88` 和同一游戏服 `223.109.142.26:10000`。
  - 账号当前主线停在 `30005 newest=100010`，尚未到击杀任务 `100030`；主城空闲态怪数 0，不能本地造怪。

## 必读

1. `AGENTS.md`
2. `.github/copilot-instructions.md`
3. `Docs/Shenxiao编码规范.md`
4. `Docs/Shenxiao重构实施方案.md`
5. `Docs/LayaUI转换流水线.md`
6. `Docs/Shenxiao进游戏链路.md`
7. `Docs/RuntimeCompare/MainQuest-DamageRootCause-第12轮.md`
8. `Docs/RuntimeCompare/MainQuest-FightVoDamage-第10轮.md`
9. 当前 Unity 代码:
   - `Assets/Scripts/Module/Core/Task/TaskModel.cs`
   - `Assets/Scripts/Module/Core/Scene/FightController.cs`
   - `Assets/Scripts/Module/Core/Scene/SceneCombat.cs`
   - `Assets/Scripts/Module/Core/Scene/SceneController.cs`
   - `Assets/Scripts/Module/Core/Scene/SceneManager.cs`
   - `Assets/Scripts/Module/Core/Scene/MonsterRenderer.cs`
   - `Assets/Scripts/Module/Core/Scene/Vo/FightVo.cs`
   - `Assets/Scripts/Module/Core/Role/*`
   - `Assets/Scripts/Module/Core/Bag/*`
10. 老端源码中至少查清:
   - 主线 `100010 -> 100020 -> 100030` 的 `TaskModel.DoTask` / `30004` / `30005` / `30000` 推进链。
   - 背包/奖励/装备/穿戴/属性刷新相关: `15010`、`13033`、装备穿戴协议、角色属性 UI 或背包入口。
   - 战斗正伤害路径: `20024`、`20001`、`FightVo`、`100030` 击杀推进协议。

## 本轮目标

把“知道为什么 0 伤害”推进成玩家可见的正向闭环:

`当前冒烟账号主线 100010 -> 合法推进到 100030 或含怪阶段 -> 通过真实奖励/装备/升级/GM 专用测试手段让 att 能破防 -> 真连攻击 -> 服务器返回 damage>0/hp变化/death -> 既有血量链显示扣血/死亡 -> 100030 击杀进度真实推进`

本轮核心是**正伤害样本与任务闭环**。不再继续分析为什么 0 伤害，除非推进/养成路径本身卡住。

## 硬性边界

- 不允许本地伪造伤害、扣血、死亡、掉落、装备、属性或任务进度。
- 不允许为了截图直接改 `MonsterVo.Hp`、`FightVo.damage`、`RoleModel.BattleAttr` 或任务计数。
- 不允许修改真实用户账号；只允许使用既有冒烟账号 `unity_npc_475823114` 或新建专用测试账号。
- 如使用 GM/API/数据库/调试接口给测试账号升级、发装备或加属性，必须:
  - 先查清接口来源和参数，不猜 endpoint。
  - 只作用于专用测试账号/角色。
  - 在报告中记录请求、响应、前后属性变化。
  - 不把 GM hack 写进客户端业务代码。
- 优先使用真实游戏链路推进任务/领取奖励/穿装备；GM 只作为测试服专用 unblocker，且必须有证据。
- 不允许 hardcode `1129`、`1134`、`59100001`、`5374/2672`、怪物 id 等调试样本进业务逻辑。
- 不允许绕过 `NetManager`，不允许独立 socket。
- 不允许提交 `.playwright-cli/`、`output/`、字体 SDF、Generated/Bind。
- `AppConfig.asset` 如需临时打开冒烟账号/自动登录，提交前必须还原。

## P0: 保护第12轮基线

先复核并保护:

- `dotnet build yu_client_unity.slnx -v:minimal` 必须通过，或明确说明不可运行原因。
- 第12轮 `RoleModel.BattleAttr` 读取链、`20024/20001` C2S、`FightVo` 解析和怪物渲染不能退化。
- `AppConfig.asset`、`.playwright-cli/`、`output/` 不入库。
- 若真连时临时改变账号状态，必须记录前后状态，且只改测试账号。

## P1: 合法推进当前账号到可打怪阶段

优先用真实客户端链路推进，而不是直接造状态:

- 真连进入当前冒烟账号，确认 `30005 newest=100010`、当前场景、任务列表、背包和角色属性。
- 点击/调用真实任务链完成 `100010`，推进 `100020`，再进入或接近 `100030`。
- 如果任务推进需要 NPC 对话/奖励弹层/背包/穿戴，必须走已有 `TaskModel`、`DialogueController`、`TaskFinishView`、`BagModel` 等真实链路。
- 如果卡住，报告卡在哪个协议或 UI，不本地改任务。

验收:

- 报告写入任务推进前后 `30000/30001/30004/30005` 或等价日志证据。
- 若成功到 `100030`，记录可见任务项、目标怪 `10001001`、真实场景坐标/怪物下发。

## P2: 真实提升攻击力或取得可破防状态

按优先级尝试:

1. **真实奖励/装备路径**:检查 `100010/100020` 奖励、背包 `15010`、装备穿戴协议、角色属性 `13033` 或角色面板更新，尝试通过真实穿戴/升级提升 `att`。
2. **新建专用角色路径**:如果当前账号状态异常，允许新建测试账号/角色，从老端/Unity同后端正常主线跑到有怪阶段，记录全链路。
3. **测试服 GM unblocker**:如果真实链路短期无法给正伤害样本，查 GM/API/调试工具是否有发装备/加属性/升级接口；只作用于专用测试账号，并记录接口证据。

验收:

- `RoleModel.BattleAttr.Get("att")` 或老端/Unity等价属性在前后有证据变化。
- 如果提升失败，必须明确是协议未接、服务端拒绝、缺接口、UI未完成，还是账号阶段不支持。

## P3: 正伤害真连验证

在 P1/P2 之后:

- 真实进入有怪场景，确保 `SceneManager` 出现真实 `MonsterVo` 且 GameView/Renderer 可见。
- 使用真实技能点击/主线自动打怪链触发 `20024/20001`。
- 抓 `20024/20001 C2S` 与 `20001 S2C`。
- 若出现 `damage>0`、`hp<max` 或 `hp==0`，必须记录防御者 id、hp、damage、damage_flag、可见血条/死亡截图或 Unity 运行态日志。
- 若仍 `damage=0`，记录前后属性、怪物配置、C2S/S2C、服务端 remote-close/限频状态；禁止本地扣血。

## P4: `100030` 击杀进度闭环

只有真实 `hp==0` 或服务器死亡/任务推送后才验证:

- `100030` 降服 3 只是否由真实死亡或服务器任务推送推进。
- 如果收到 `30000/30001/30005` 或其他任务推送，记录协议证据。
- 如果击杀可见但任务不推进，记录真实 blocker，不本地改任务。

## P5: 只记录不扩散

只记录，不编码:

- 完整自动战斗 AI、挂机寻怪循环。
- 完整动作系统、完整 CD、完整特效、音效、伤害飘字。
- 完整装备系统/强化/背包全部页签/角色属性完整 UI。
- PvP、伙伴/神祇/天赋、掉落、活动副本、BOSS、队伍协同。

## 验收与提交

1. 建立 `Docs/RuntimeCompare/MainQuest-PositiveDamage-第13轮.md`。
2. `dotnet build yu_client_unity.slnx -v:minimal` 通过，或说明真实不可运行原因。
3. 报告必须明确:
   - 当前账号是否从 `100010` 推进到 `100030` 或有怪阶段。
   - `att/wreck/def/Hp/level/power` 前后变化。
   - 是否通过真实奖励/装备/升级/GM 专用测试手段提升攻击力。
   - 是否出现真实 `damage>0/hp/death`。
   - `100030` 是否真实推进。
   - 哪些代码改了，为什么必须改；若没改代码，说明边界。
   - 下一轮最小可行动作。
4. 只提交本轮任务包、报告、必要代码，不带 `.playwright-cli/`、`output/`、字体 SDF、Generated/Bind、临时 AppConfig。
