# 共鸣模块 UI 精修与路由验收（2026-08-07）

> 状态：Prefab、配置和功能链已落地；材料组居中、共享物品详情布局已修。最新人工复查进一步纠正了流光归属：它是明确 opt-in 的已穿戴装备槽状态，不属于共鸣中央当前/下一阶图标；同时确认共享 `EquipmentItem`/`BaseAwardItem` 根组件丢失导致背包格回归。现已恢复共享根、拆开槽位与页面特效倍率，并补充代表性消费者门禁。Core/Editor 串行编译 0 error，但修后 Unity 运行态与真实 Web 均尚未复验。账号资产写事务最终确认和条件礼包入口也仍未收口。父路由保持 `blocked`，不得宣称整页 `done`。
>
> 适用入口：人物 → 共鸣
>
> 当前事实源：`Assets/Prefabs/UI/Suit/SuitModule.prefab`。运行时代码只负责数据、事件、协议、点击语义和必要显隐，不重建人工 Prefab。

## 一、本轮结论

本轮把原先只有老端 `EquipSuitBaseView`、Unity 没有可编辑 Prefab 的共鸣模块首次落成，并按 UI 精修口径完成老端事实枚举、逐控件实现和首轮 Editor 深度优先复验。首轮覆盖四个共鸣页签、22 个装备部位、46 档属性、40 个材料详情、说明弹窗、18 个特效预览、22 个回退预览/取消链、打造确认/取消链、红点、常驻特效、成功特效、返回、关闭重开、配置与协议状态刷新。随后人工运行态复查推翻了三类视觉结论；修复已落地，但新运行证据产生前不得沿用首轮绿色结果。

当前 458 节点台账状态如下：

| 状态 | 数量 | 含义 |
|---|---:|---|
| `done` | 264 | 未被本次人工证据推翻，且已有老端事实和 Unity Editor 真实点击/渲染或静态协议证据的叶子 |
| `blocked` | 80 | 44 个真实 15221/15222 最终确认叶、22 个部位父页、4 个部位容器、4 个页签父页、4 个条件礼包叶、页签总页和模块根页；后六类主要是子节点未收口后的状态传播，不是 80 个独立功能缺口 |
| `needs-runtime-verify` | 114 | 原有 3 个 Web/适配/性能叶、人工复查重开的 66 个目标路线叶（22 个部位展示、22 个材料组展示、22 个材料详情）及 44 个材料相关父节点，再加上全局特效叶；这些是共鸣路线节点，不表示要把共享格的全部外部消费者逐页重跑。修后必须按消费形态抽样运行，不能用编译或旧截图代替 |

历史台账位于 `output/ui_route_audit/2026-08-07_resonance/route-ledger.json`。它是 schema 4 的 2026-08-07 状态快照，可继续读取本报告的 `done/blocked/needs-runtime-verify` 边界，但不得在后续复验中原地抬版本或重放旧绿灯。manifest 仍由 `Tools/Conversion/build_resonance_route_manifest.cjs` 生成；下一轮从该 manifest 用通用 `route_ledger.py init` 新建 schema 6 台账，再以本轮 results 原子 apply。

## 二、人工复查后的共享组件回卷

三轮人工复查及最新归属纠正位于 `output/ui_route_audit/2026-08-07_resonance/user_review_2026-08-07_1550/`、`user_review_2026-08-07_1632_flow_frame/` 和 `user_review_2026-08-07_1705_slot_ownership/`。本次不是按 22 个部位复制修补，也不是把共享格的全部引用页逐一重跑，而是按共享身份、消费形态和代表样本处理：

1. 部位装备格缺共鸣流光框：共享 `EquipmentItem` 保留 `SetSuitEffect` 和 `effBox/effBox1/effBox2` 槽位宿主，槽位资源倍率仍由该共享组件负责；但最新截图证明把同一数值机械写入 `ResonancePresenter` 会让共鸣中央上下图标出现错误贴身框。现在只有共鸣边缘部位格和 `BagEquipmentIcon` 在“传入实例正是该部位当前已穿戴实例、共鸣状态满足”时显式 opt-in；材料、奖励、详情、普通背包格、中央当前/下一阶和预览默认不拥有 `_suitEffect`。中央与预览继续使用页面自己的 `effBox1/effBox2/preview.effBox` 和独立倍率映射。
2. 材料组从左向右顶格：`SuitModule.prefab` 的真实 `costList.content` 改为 preferred width、中心锚点和中心 pivot；1/2/3 个 `EquipSuitCostItem` 按整体宽度居中，内部仍复用 `_tpl_EquipmentItem`，不创建页面专用物品格。
3. 共享物品详情类型换行、确定按钮偏左：一次修改 `CommonModule.prefab/GoodsTooltips`，类型与数量栏统一扩到 240px，`btn_group` 使用不拉伸子项的 `HorizontalLayoutGroup` 居中单/双按钮；删除 `ItemTipsView` 对数量栏的运行时坐标补丁，视觉参数继续以 Prefab 为事实源。
4. 背包大面积空灰格：源码差异确认 `EquipmentItem.prefab` 根上的 `EquipmentItem` 和 `BaseAwardItem.prefab` 根上的 `BaseAwardItem` 曾被删除，导致 `BagEquipmentIcon.GetComponent<EquipmentItem>()` 与 `BagItemRenderer.GetComponent<BaseAwardItem>()` 都取不到组件。现只恢复两个共享根组件和原序列化引用；`EquipmentItem.prefab` 已确认的两个空 Image 透明修正继续保留，没有重转或覆盖共享 Prefab。
5. 2026-08-08 背包截图再次推翻品质流光结果：这次不是共鸣 `ui_shenzhuang*` 的归属问题，而是共享槽自身的 `ui_goods_* / UI_1309 / UI_1310` 动画在全屏共享 RT 中失去老端实例裁切；同时 `BaseAwardItem.effect_con` 为 0×0 且停在左上角。现保留老端 `scale=14` 和原动画资源，只在六个精确 profile 恢复 `clipToRenderRect`，把共享宿主改为非零居中矩形，并停止从 View 传入脱离宿主的固定 `renderSize`。本条会重开背包普通格、已穿戴格和共鸣边缘槽代表样本，但不重开共鸣中央页面特效所有权结论。
6. 同日第二轮截图再次推翻“位置正确即可”的判断：六套动画只写 `material._BaseMap_ST`，27 个闭包内材质却未开启 `_UseBaseMapST`，所以框仍是静止首帧；同时共享通道挂在页面根，未继承背包 `Viewport/RectMask2D`，造成滚出列表的物品只剩流光框压到底栏。当前已定向启用这 27 个材质的 BaseMap UV 分支，并在 `UIEffectStage` 中把槽位边界与有效祖先 `RectMask2D/Mask` 求交，完全离开时隐藏 wrapper。运行态必须新增同一 Handle 双时间点像素差以及完整/部分/完全滚出三态证据；在这些证据产生前继续保持 `needs-runtime-verify`。
7. 用户随后在真实运行态确认修后效果“好了”：品质框已持续流动，底部滚动裁切也不再残留孤框。该确认关闭本次背包代表样本暴露的静态与 viewport 泄漏缺陷；由于本轮未由自动路线保存双时间点 PNG/像素差，也没有重新跑普通详情、装备详情及全部代表宿主，专项总路线仍按未覆盖项分别保留状态，不能把一句人工确认扩写成整条共享详情路线全部 `done`。

`consumer-inventory.md` 已按源 Prefab GUID 静态列出 `EquipmentItem` 81 个、`BaseAwardItem` 76 个直接 Prefab 消费者；`ResonanceCase` 已增加共享根组件、共鸣模板，以及背包装备位与普通背包格两个高频代表消费者的静态断言，既有 `BagInteractionCase` 也加入同一组共享根/嵌套模板完整性门禁，避免今后只跑背包专项时漏掉相同回归；`ResonanceRouteCase` 已增加 1/2/3 项实际居中、类型文字单行、单按钮居中、部位特效精确 Handle/二维足迹/关闭清理，以及“中央展示不得拥有槽位 `_suitEffect`、页面特效不得套用槽位倍率”的断言。当前运行样本固定为共鸣边缘槽、背包装备槽、普通背包格和共鸣中央展示；样本失败才扩大同组抽查，公共 API/序列化字段删改或整体替换引用时才要求全量消费者核对。验收程序集已编译通过，但本轮没有启动或控制 Unity，所以新增断言仍是“已编译、未执行”。

## 三、老 H5 权威事实

同账号 `111111` 的老 H5 后台采集位于 `output/ui_route_audit/2026-08-07_resonance/old_full/`。本轮没有使用前台鼠标键盘。

- 四个页签依次为妖魂、战魂、万物、饰物，对应部位数 `6/6/6/4`，总计 22 格。
- 四页属性阶段分别为 `16/10/8/12`，总计 46 档。
- 当前账号所有部位显示已穿戴装备，但共鸣等级为 0，因此老端没有可执行的回退状态；回退页由精确 15223 注入态覆盖结构、奖励列表、关闭和取消路径。
- 说明按钮使用老端 `InstructionType.EquipSuit=1524`。历史占位 `453` 是人物属性说明，已纠正，禁止串用。
- `EnumPushGiftType.eGongMing` 当前账号不显示；Unity 也没有该类型的权威数据切片与购买页，入口保持隐藏，不用任意礼包冒充。
- 老端主动调用点只消费通用 `ui/2_dianji`，没有共鸣专属声音。Unity 继续走既有全局按钮音消费者，不伪造打造成功音或回退结果音。
- 老端装备格实际使用 `com_goods_plate_4` 展示框，而配置/业务品质仍为 7。Unity 显示层固定 `SetDisplayColor(4)`，业务条件继续保留真实 `Color=7`，两者不得混用。

## 四、Prefab、布局与资源落地

- 新增可编辑 `Assets/Prefabs/UI/Suit/SuitModule.prefab` 及完整 Bind；页签、内容、说明、预览、回退弹层和遮罩直接保存到 Prefab。
- 部位区使用两列 `GridLayoutGroup`，格尺寸 `112×200`、横向间距 450；属性区使用三列横向布局。四个共享内容页的滚动结构均为 `ScrollRect → Viewport/RectMask2D → Content/GridLayoutGroup`。
- 老端“万物共鸣”同时显示三种材料。初版材料视口宽 200，第三格虽生成但被裁剪；最终改为 `x=-160,width=320`，静态门禁确认 `viewport=320, need=319`，Editor 截图可见三格并可逐格打开详情。
- 最初完整路线虽通过，但人眼对照仍发现材料数量被内层 `EquipmentItem` 重复绘制、属性字号/颜色偏离老端，以及通用材料详情的数量栏沿用转换 JSON 的 66px 初始宽度。最终按老端 `EquipSuitCostItem/EquipNewSuitAttrItem/GoodsTooltips` 运行态恢复为：外层唯一 `have/need`、材料字 18、属性字 20/宽 200/行距 6、激活色 `#0A953E`、未激活色 `#676767`，详情数量栏 `x=120,width=240`。这些规则已进入 Editor 路线的 240 项视觉文字/详情布局门禁，不能只凭一次绿色结果跳过人眼复查。
- 动态装备格优先把稳定的 `item_bg` 作为唯一点击面，避免异步图标显隐导致射线面消失。预览关闭、回退遮罩/关闭/取消/确认、通用物品详情确定按钮均有明确射线面。
- 新增并纳入 `Remote_suit` 的三张同源配置：`config_equip_pos2suittype` 10 项、`config_equip_suit_item` 46 项、`config_equip_suit_make` 252 项；配置不完整或解析失败时不换入旧快照。
- 共鸣边缘已穿戴装备槽按状态使用 `ui_shenzhuang01/02/03`，中央当前/下一阶与预览走页面独立特效链，打造成功使用 `ui_gongmingchenggong`；共享格在数据重绑、父页面失活和销毁时释放槽位 Handle，页面 Presenter 单独负责自己的切格、切页、关闭和重开清理，禁止两个所有者互相代管。

## 五、15221/15222 事务闭环

`15221` 打造和 `15222` 回退已从 hard-negative 中移除并完整实现；`15218` 神装升阶仍保持 hard-negative。本轮没有向真实账号发出任何 15221/15222 写帧。

事务共同约束：

1. 页面必须等待三张配置、角色、已穿装备、背包和 15220 共鸣快照就绪。
2. 点击时冻结页签、部位、配置、装备与材料指纹；二次确认时重新计算，状态变化即拒绝发送。
3. 同一页面保持单飞，统一处理 15200 错误和 12 秒超时；失败或超时保留旧模型并释放交互。
4. 成功回包先按权威结果局部换入，再立即重拉 15220、背包和已穿装备；15222 额外重拉邮件，并接受换装不兼容时的服务端主动 15222 推送。
5. 禁止乐观扣物、返物或本地发奖；没有最终确认授权时，真实成功叶必须保持 `blocked`。

静态协议专项覆盖 8 个装备请求帧、8 个背包刷新帧、6 份结果和 4 次模型更新；状态全部恢复。协议覆盖 A–M 门禁报告为 `Reports/ProtocolCoverage/coverage_20260807_121650.md`，当前 152 族仍因 15218 保持 `pending`，冻结 baseline 计数未被改写。

## 六、验证证据

### 6.1 Editor 全路由

最终不可变证据目录：`output/ui_route_audit/2026-08-07_resonance/unity_editor/2026-08-07_1452_instruction_final/`

| 门禁 | 结果 |
|---|---|
| 部位与阶段 | `positions=22/22`，`stages=46/46` |
| 逐格点击 | `positionCells=22/22` |
| 材料详情 | `materialDetails=40/40` |
| 视觉文字与详情布局 | `visualTypography=240`；材料无重复数量，属性字号/颜色正确，详情数量栏 `x=120,width=240` |
| 说明弹窗 | 7 条规则完整，`instructionLayout=True`、`instructionLastReachable=True`、`instructionScrolled=False`；当前正文一屏可达且相邻文字零重叠 |
| 打造确认取消 | `buildCancels=22` |
| 特效预览 | `previews=18` |
| 回退预览/关闭 | `returns=22` |
| 读协议 | `15220×6`、`15223×22`、背包帧 12；`15221×0`、`15222×0` |
| 旧特效 RT 任意 alpha 像素（流光框门禁已失效） | 四页分别 `18/35/62/57`；少量像素不能证明完整槽位流光，且该证据没有区分槽位特效与页面中央特效所有者，不能继续作为本问题的通过依据；成功特效 `16674` 未被本次证据推翻 |
| 性能 | 最终同进程复跑 Editor 冷开 1886ms、热开 146ms；首次完整证据轮为 6488ms/1031ms，均不替代真实 Web |
| 现场恢复 | `restored=true` |
| 结论 | `code=0, verdict=pass` |

Editor 路由用例真实通过 `GraphicRaycaster → PointerClick`，滚动前会渲染一帧以提交动态 Graphic depth；所有最终截图写入独立目录，没有覆盖旧证据。首轮绿色结果经人眼复核后继续修正了材料重复数量、属性字号/颜色、`GoodsTooltips` 数量栏 66px 换行，以及说明弹窗在最终 CJK 字体下第 3～6 条重叠；最终门禁会在字体就绪后重新排版，并检查相邻文字零重叠和末项可达。

### 6.2 静态门禁

`CliVerify.Resonance` 最终为 `code=0, verdict=pass`：

- 配置：`10/46/252`，生产解析器与生产校验器。
- Prefab：Bind、弹层、8 个 ScrollRect/4 个路由内容页、三材料视口、模板/激活态和分隔图均通过。
- Addressables：`Remote_suit` 中 Prefab 与三张配置地址全部存在。
- 协议：8 个请求帧、8 个背包刷新、6 份结果、4 次更新，验证后模型与发送器现场恢复。

稳定结果文件：

- `output/ui_route_audit/2026-08-07_resonance/unity_editor/2026-08-07_1452_instruction_final/route-result.txt`
- `output/ui_route_audit/2026-08-07_resonance/unity_editor/2026-08-07_1452_instruction_final/static-result.txt`
- `output/ui_route_audit/2026-08-07_resonance/unity_editor/2026-08-07_1452_instruction_final/fingerprints.sha256`

上面的 Editor/静态结果均属于人工复查前的不可变证据，仍可证明未被推翻的路线和协议边界，但不能证明本次共享根、槽位特效归属、材料居中和详情布局修复。尤其是原先 `>=8` 的特效像素条件没有隔离所有者，旧 `18/35/62/57` 不得继承为槽位流光证据。当前源码的 Core/Editor 工程已串行编译到 0 error，通用台账自测、458 节点实际台账校验及共享根/代表引用静态检查通过；新的 Unity 运行态目录尚未生成。

## 七、明确未执行与下一步

以下四项不能由本轮离线编译替代：

1. **共享格代表性运行抽查**：先在新证据目录验共鸣边缘已穿戴槽有完整流光、中央当前/下一阶无槽位贴身框，再验背包装备槽正常显示并按状态开/关流光、普通背包格正常显示且保持 off；品质流光在同一 Handle 上至少保存两个不同时间点且像素发生变化的画面，并把背包格完整可见、底部部分裁切、完全滚出后零残留三态一起验证。四种宿主形态和动态/裁剪证据都通过才可收口，失败才扩大同组抽查。
2. **真实 WebGL 同账号像素对比**：当前 Editor 构建目标是 Android。切换 WebGL 会触发大量平台资源导入，本轮没有获得切换、完整内容构建和发布授权，因此没有 Player/catalog 哈希，也没有 old/unity/overlay/diff。后续应在持续 Web 构建工作区成套构建内容和壳，固定 720×1280 与 1920×1080，按同账号顺序重走全部适用叶子。
3. **真实账号最终确认**：15221/15222 会改变装备、材料和属性。只有用户明确授权具体账号和可消耗范围后，才能各做一笔成功路径，并核对即时父页刷新、关闭重开和资产账。
4. **条件共鸣礼包**：需要先补 `eGongMing` 的权威数据切片、入口条件和实际购买目标，再用有该状态的账号验显示、跳转、关闭和生命周期；当前保持隐藏是正确行为。

## 八、可重跑入口

- 老端事实采集：`node Tools/Conversion/resonance_login_capture.cjs`
- 路由初建：`node Tools/Conversion/build_resonance_route_manifest.cjs`
- schema 6 新账：`python .agents/skills/audit-game-ui-route/scripts/route_ledger.py init <route-manifest.json> <new-route-ledger.json>`
- 当前证据回填与父状态传播：`python .agents/skills/audit-game-ui-route/scripts/route_ledger.py apply <new-route-ledger.json> <current-results.json>`
- 历史更新器：`Tools/Conversion/update_resonance_route_ledger.cjs` 已封存，默认拒绝运行；只允许 `--legacy-replay --output <candidate.json>` 生成独立候选，且候选路径不得是正式账。
- Unity 静态专项：`Assets/Editor/CliVerify/Cases/ResonanceCase.cs`
- Unity 全路由专项：`Assets/Editor/CliVerify/Cases/ResonanceRouteCase.cs`

任何后续结果都必须写入新的不可变 run 目录，记录 SHA-256 和 verification run，并走通用原子 `apply`；不得只改父节点状态、直接写正式账或用截图数量替代逐叶结果。
