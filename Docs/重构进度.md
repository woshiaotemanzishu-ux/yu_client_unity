# yu_client_unity 客户端重构 · 进度与路线图（永久任务）

> **目标**：Unity 端完全照抄老客户端 yu_client（Laya），做到可玩对齐。
> **看进度方式**：用户在 Unity Editor Play 实玩验收里程碑，不读代码/日志。
> **UI 规范**：见同目录 `UI模块SOP.md`（铁律：我管结构、用户管效果）。

## 续接协议（断了 / 换 context 后，我开局先读这里）
1. 读下面「当前焦点」，立刻知道续在哪。
2. `git log --oneline -20` 看最近检查点（commit 里带 `[chunk X]`/模块名）。
3. **只在"能编译"的状态提交**；绝不留半残。一页/一模块一个 commit = 安全续点。
4. 碰 Unity 的操作（生成预制体/编译/MCP 探针）**串行**——Unity 单实例，子 agent 只做只读研究。
5. 推进顺序：**先地基（功能/公用件），后模块（业务页）**。

## 当前焦点（UI 阶段 · 2026-06-19 起）

**目标**：按 `UI模块SOP.md` + LoginModule 标准，**从主界面 MainUI 开始，一个个把未接入的 UI 接进来**，最终完全复刻 Laya 客户端 UI。基础设施（场景/资源/登录/数据层）已就绪。

**标准（铁律，每个 View 都遵守）**：
- **我管结构，用户管效果**：我交付 `{X}View.cs : {X}ViewBind`（OnInit 接点击/订阅事件、OnShow 刷数据、OnDispose 退订），**只写逻辑不写布局**；位置/大小/颜色/图归用户在预制体调。
- **不重转 prefab**：Bind 已生成=预制体已存在，**绝不重跑 UICreator**（会覆盖用户手调），只新增/改 `Views/{X}View.cs`。
- **优雅降级**：后端未移植的（协议/Model 没有）→ 点击打日志「待对接 XXX」、动态元素先隐藏、文案给默认，**不崩**。
- **对照 Laya**：行为抄 `yu_client/h5/src/mainUI/{X}.ts`；事件名/Model 用 Unity 侧已有的（`GlobalEvent.EVT_*`）。
- **MCP 编译验证**（ValidateScript/RunCommand）→ 能编译才提交；**一个 View 一个 commit**。

**MainUI 接入队列（未接入 Bind=预制体在但没 View 逻辑；做完划掉）**：
- 架构注：MainUI 走轻量 `MainUIFlow`（无 Controller/Model/Bootstrap，刻意）；9 个首屏 View 已接（Top/Activity/Skill/Chat/Secondary/TaskTeam/Down/AutoBrush/JoyStick）。事件驱动弹层默认关、不进 FirstPass。
- **P1 核心**：✅ MainUIReliveView（复活窗，本地倒计时降级，待死亡链路）｜✅ MainUIBuffView+MainUIBuffItem（Buff详情，空列表降级，待 Buff 数据源）｜✅ MainUIFightModeView+MainUIFightModeItem（战斗模式，空列表降级，待 PkStatusModel/协议）｜⬜ MainUISkillItem+MainUISkillItemGod（技能项,补技能条）
- **接入机制(本轮确认)**：未接入 View=**独立 prefab**(`Assets/Prefabs/UI/MainUI/{X}View.prefab`，`/Assets/Prefabs/UI/` 已 gitignore=本地产物不提交)，非合并 MainUIModule 子节点(那只含 9 个常驻 HUD)。流程：①写 `Views/{X}View.cs:{X}ViewBind`→②MCP 编译(改 .cs 触发域重载,先 GetState 重连)→③`RunCommand: LayaBindFiller.FillModule("mainUI")` 把 View 组件挂到本地 prefab(Bind→View+回填字段，不动布局)→④RunCommand 验证组件挂上+字段绑定→⑤**只 commit .cs**(prefab gitignore)。事件驱动弹层暂不接打开入口(待对应协议/事件移植)。
- **P2 战斗反馈**：⬜ MainUIHiterBigBloodView（BOSS血条）｜⬜ FightingUpView（战力提升）｜⬜ MainUIEffectView+MainUIEffectPartnerSkillView（特效容器）｜⬜ OpenDestinyView（命运窗）
- **P3 功能完善**：⬜ MainUIChatItem（聊天项,需ChatModel）｜⬜ MainUIIconBoxView（功能图标集）｜⬜ DropProgress（掉落/采集进度）｜⬜ FuncBoardView（功能气泡）｜⬜ MainUITopBuffItem（顶部Buff行）
- **P4 装饰/可选**：⬜ ArrowComponent｜⬜ TalkBoard｜⬜ FlowerEffectView（婚礼花）｜⬜ MainUIMarriageItem｜⬜ EyouThAdultItem｜⬜ TopPlayerTipItem｜⬜ MainFuncIconItem
- **MainUI 做完 → 其他模块**（背包/角色/装备/公会/活动…各自 UICreator 生成 Bind 后同法接入）。

**续点机制**：每轮读本节队列取**下一个 ⬜**，做完标 ✅（带 commit 短哈希）。范本 = `Module/Core/MainUI/Views/MainUIAutoBrushView.cs`（事件订阅+刷新+降级范例）、`MainUIDownView.cs`（动态克隆模板范例）。

---

## 资源阶段（✅ 已完成，2026-06-19）

> **🚦 阶段状态（资源转换已验收完成，2026-06-19）：✅ 转换正确，仅 faqi 待接。**
> - **MCP 恢复后已完成全链路收尾**：
>   1. ✅ **C# 动作目录修复已应用+实编译验证**（`AssetHubDomains.ScanObjsDir` 改读 .lh clipPath，commit `01b3a93fe`）。MCP ValidateScript 0 诊断 + RunCommand 实跑确认解析正确。
>   2. ✅ **重转 85 个动作错位模型**（head58/back12/child8/other4/mount3，commit `4d4ef4f1b`，0 失败）。
>   3. ✅ **全量复审**：17 域 prefab 解析实际 clip 源目录 vs .lh 权威目录 → **动画正确 554 / 无动画 0 / 错目录 0**；结构 602 prefab 全 mesh+mat+import.json+tool v5；in-engine 抽查 head_1302/back_1001/mount_1021/child_1031/other_401417 均 mesh+mat+clips 齐全。
> - **用户已批量转所有域**：覆盖率 100%（role92/head124/weapon43/back14/wing22/mount38/monster118/npc35/pet20/spirit23/child12/fabao12/god12/ghost5/fairy4/littlepet23/other5 + 特效1750）。
> - **唯一遗留**：**faqi(26)** 动作在 `res/model_faqi_X/action/` 非标基路径，`ScanObjsDir` 的 ActionDir 基路径(`object/faqi/action/`)对不上,需定制 ActionDir 解析才能接——优先级低，留待需要法器时做。
> - **待用户肉眼验收**：Editor 里抽查动作播放对不对（如 head_1302 应播 1102 动作）、各模型渲染对不对（贴图/朝向/大小）。

**目标顺序**：① 把转换工具 `tools/yu-resource-tool`「修好」→ ② 转出所有资源域(**可渲染**) → ③ 逐个界面接后端(模板 `Module/Core/Login` 细粒度 / `MainUI` 大致OK) → ④ 用户与我逐模块细化。终极目标 = 完全复刻。

**已知(有据)**：转换链是通的——`Assets/GameRes/object/role/model_clothe_1111/` 一套完整(prefab+mesh+mat+jpg+import.json)且能渲染；特效域 1750/1750 已转。**所以不是从零，是补齐/修特定域。**

**⚠ 管线真相（2026-06-19 自走只读诊断，纠正旧理解）**：
- **真正的 Laya→Unity 模型转换器在 Unity C# 侧**：`yu_client_unity/Assets/Editor/AssetHub/`，窗口菜单 **`神霄/资产管理`**(`AssetHubWindow`)；模型走 `Laya3DImporter.cs`、特效走 `LayaEffectImporter.cs`。**域清单/开关在 `AssetHubDomains.cs`**。产物 = `Assets/GameRes/object/{域}/{name}/{name}.prefab + _mesh.asset + .mat + .import.json`。
- **上游 GLB 由 Python `tools/yu-resource-tool/python/laya_to_glb.py` 产出**(读真实 .lh/.lm/.lmat/.lani → 带动画 .glb)。**已实测**：在真实 `model_mount_1000` 上导出齐全(48 骨骼/4 动画 idle·run·show·show2/网格+材质+贴图)。venv 在 `python/.venv`(含 pygltflib)。
- **`glb_to_role/head/weapon_pack.py`、`make_*.py` 是「FBX→Laya 老客户端」作者工具(方向相反)，不是 Unity 转换器**——做 Unity 资源别再以为是它们。

**资源域状态表（2026-06-19 自走只读诊断；"已转"=Unity prefab 已生成数；"渲染?"仍需实机肉眼验，CLAUDE.md 的坑）：**
| 域 | 已转(prefab) | 状态 | 渲染? |
|---|---|---|---|
| 角色时装 | 92 | ✅ 域启用+已批量转 | 1111 确认；余待实机 |
| 创角默认装 | 含 role(4 职业) | ✅ 域启用 | 1111 确认渲染 |
| 头饰 head | 124 | ✅ 已转;但 **58/124 共用动作**(旧逻辑→无动画),待 C# 修后重转 | 待实机(查动作) |
| 武器 weapon | 43 | ✅ 域启用+已批量转(0 共用动作) | 待实机 |
| 背饰 back | 14 | ✅ 已转;但 **12/14 共用动作**(旧逻辑→无动画),待 C# 修后重转 | 待实机(查动作) |
| 翅膀 wing | 22 | ✅ 域启用+已批量转(0 共用动作) | 待实机 |
| 坐骑 mount | 0 → **本轮启用域** | 🔶 已启用+上游全 38 验通；动作解析 Python已修/C#待修(3个共用动作) | 待批量转+实机 |
| 特效 effect | 1750/1750 ✓ | ✅ | 待实机 |
| 实体域(怪/NPC/宠物等9域) | 0 → **第4轮接入** | 🔶 ~241模型已启用;失配仅 child 8(待C#修),其余动作映射正确 | 待批量转+实机(M1需怪/NPC) |
| 小宠 littlepet | 0 → **第6轮接入** | 🔶 23模型,动作标准0失配 | 待批量转+实机 |
| 杂项 other | 0 → **第6轮接入** | 🔶 5模型,4个共用动作(待C#修) | 待批量转+实机 |
| 法器 faqi | 0 | ⛔ 非标res/action布局,暂不接,待C#读.lh clipPath后接 | — |
| (fashion) | — | 贴图变体非模型域,不接 | — |

> **结论**：除坐骑外所有模型域均「已启用+已批量转出 prefab」；**坐骑是唯一未转域**，根因仅是 `AssetHubDomains` 里它 `Enabled=false`(gated「先验真实样本」)。本轮已验样本(上游 GLB ✓)并启用域 → 坐骑现可在 `神霄/资产管理` 扫描+批量转。

**第 2 轮(2026-06-19)批量验证 + 共用动作目录修复**：
- **全部 38 个坐骑经 `laya_to_glb` 导出 0 失败**(批量实跑),贴图齐全。动画分布:34 个=4 动画(idle/run/show/show2)、1 个(1040)=2 动画(本就如此)、3 个(1021/1022/1023)曾=0 动画。
- **找到共用动作目录坑**:`model_mount_1021/1022/1023` 的 `.lh` 实际引用 `action/1007/1008/1009`,而**文件名启发式(找 action/{自身id})落空** → 这 3 个会被转成「无动画」。**只有 .lh 里的 Animator clipPath 是权威来源**。此坑对**所有部件域**潜在通用(任何共用别 id 动作的模型)。
- **已修(Python 侧,已验)**:`ed5601ba1` `[坐骑域/动作解析]` — `main.py` 新增 `_lani_from_lh()` 读 .lh clipPath 权威解析,`export-glb` 优先用它(回退原启发式)。实测 1021→1007、1022→1008、1023→1009 恢复 4 动画。**仅作用于 Python 预览/导出**。
- **⚠ 待修(C# 侧,需 Unity MCP 在线编译验证)**:Unity 批量转换的动画来自 `AssetHubWindow.LanisFor(e)` → `Directory.GetFiles(e.ActionDir,"*.lani")`,而 `e.ActionDir` 由 `AssetHubDomains.ScanObjsDir` 用同款文件名启发式(`model_mount_1021`→`action/1021`,不存在)。**需把 `ScanObjsDir`(或 `LanisFor`)改为读 .lh 的 Animator clipPath 定动作目录**(算法已在 Python 验证)。本轮 Unity MCP 连接被吊销,build-critical 的 Editor 脚本不做无编译验证的改动 → 攒此项,MCP 恢复后做。**影响面见第 3 轮(远不止 3 个,是 ~73 个)**。

**第 3 轮(2026-06-19):贴图 .ktx 负结果 + 共用动作目录「全域爆炸半径」**：
- **贴图 .ktx 无缺口(负结果,已扫全量)**:全部 **471** 个 object 域 `.lmat` 引用的贴图后缀=`.jpg`×468 / `.png`×2,**磁盘全部存在,0 个 ktx-only**。`.ktx` 只是 Laya 运行时压缩伴生文件,`.lmat` 一律指向 `.jpg`。→ `laya_to_glb` 贴图处理对所有 object 域**无需改**。
- **共用动作目录爆炸半径(关键)**:统计各域「`.lh` 的 clipPath 动作目录 ≠ 模型自身 id」的数量(这些在旧文件名启发式下=**转出无动画**):
  - **head: 58/124**(规律 `model_head_13xx → action/11xx`),**weapon: 0/43**,**back: 12/14**(`model_back_100x → action/100(x-1)`),**wing: 0/22**,**mount: 3/38**。
  - **共 ~73 个模型**;其中 **head/back 已批量转(124/14 prefab)→ ~70 个已转 prefab 大概率缺动画**(肉眼:模型在,但不动)。weapon/wing 不受影响。
  - → **C# 修复优先级升为「高」**(质量 bug,非边角)。**修了 `ScanObjsDir` 读 .lh clipPath 后,需重转 head/back/mount 三域**。
- **Ready-to-apply C# 补丁(MCP 在线即贴,算法已 Python 验证)**——`AssetHubDomains.cs` 的 `ScanObjsDir` 里把
  `int us = name.LastIndexOf('_'); string actionDirName = us >= 0 ? name.Substring(us + 1) : name;`
  改为 `string actionDirName = ResolveActionDirName(f, name);`,并加辅助方法(用本文件已 import 的 `Newtonsoft.Json.Linq`):
  ```csharp
  // 动作目录以 .lh 内 Animator clipPath 为权威(模型常共用别 id 的动作,如 head_13xx→action/11xx、
  // back_100x→action/100(x-1)、mount_1021→action/1007);解析不到再退回文件名末段数字。
  private static string ResolveActionDirName(string lhPath, string modelName)
  {
      try {
          var data = JObject.Parse(File.ReadAllText(lhPath))["data"] as JObject;
          string cp = FindFirstClipPath(data);
          if (!string.IsNullOrEmpty(cp)) {
              cp = cp.Replace('\\', '/');
              int ai = cp.IndexOf("/action/", StringComparison.OrdinalIgnoreCase);
              if (ai >= 0) {
                  string rest = cp.Substring(ai + "/action/".Length);
                  int slash = rest.IndexOf('/');
                  if (slash > 0) return rest.Substring(0, slash);
              }
          }
      } catch { }
      int us = modelName.LastIndexOf('_');
      return us >= 0 ? modelName.Substring(us + 1) : modelName;
  }
  private static string FindFirstClipPath(JObject node)
  {
      if (node == null) return null;
      if (node["components"] is JArray comps)
          foreach (var c in comps)
              if ((string)c["type"] == "Animator" && c["layers"] is JArray layers)
                  foreach (var l in layers)
                      if (l["states"] is JArray states)
                          foreach (var s in states) {
                              string cp = (string)s["clipPath"];
                              if (!string.IsNullOrEmpty(cp)) return cp;
                          }
      if (node["child"] is JArray children)
          foreach (var ch in children) {
              string r = FindFirstClipPath(ch as JObject);
              if (r != null) return r;
          }
      return null;
  }
  ```

**第 4 轮(2026-06-19):发现并接入 9 个实体模型域(怪/NPC/宠物等)**：
- `cdn/resource/object/` 下**实体模型域此前完全没接入 AssetHub**(只接了 role/head/weapon/back/wing/mount/effect)。新增接入 9 个域、**共 ~241 个模型**:

  | 域 | objs.lh | 样本验证(laya_to_glb) | M1 关键 |
  |---|---|---|---|
  | 怪物 monster | 118 | ✅ 54骨/8动画/贴图 | ✅ 场景刷怪 |
  | NPC npc | 35 | ✅ 49骨/2动画/贴图 | ✅ 场景 NPC |
  | 宠物 pet | 20 | ✅ 66骨/7动画 | |
  | 精灵 spirit | 23 | ✅ 56骨/7动画 | |
  | 童子 child | 12 | ✅ 20骨/3动画 | |
  | 法宝 fabao | 12 | ✅ 5骨/2动画 | |
  | 神祇 god | 12 | ✅ 57骨/4动画 | |
  | 鬼魂 ghost | 5 | ✅ 24骨/2动画 | |
  | 仙灵 fairy | 4 | ✅ 11骨/1动画 | |
- **命名坑**:`monster`/`npc` 的 .lh **沿用 `model_clothe_*`**(怪=8 位 id 如 `model_clothe_10010001`、NPC=6 位 id 如 `model_clothe_100101`),与角色时装分属各自 `object/{域}/` 子目录、OutDir 也分目录 → 无冲突。其余域用各自 `model_{域}_` 前缀。
- **已接入(已 commit `[实体域]`)**:9 域全 `Enabled=true`,走相同 `ScanObjsDir` 模式(纯数据条目,无新逻辑;MCP 吊销未编译验证,结构等价于坐骑/部件域)。
- **怪物/NPC 是 M1「进城可动」场景渲染(其他玩家/怪/NPC)的前置资源** → 接入后,渲染 D/E/F 才有模型可用。

**第 5 轮(2026-06-19):全域动作目录失配统计 + 修正第 3 轮口径 + export-fbx 同步**：
- 全 15 域扫描「.lh clipPath 动作目录 ≠ 文件名末段 id」(= 文件名启发式会落空):
  role 121/125、head 58/124、back 12/14、mount 3/38、**child 8/12**;weapon/wing/monster/npc/pet/spirit/fabao/god/ghost/fairy **全 0**(失配总计 202)。monster 有 42 个无 Animator(静态怪,正常)。
- **关键修正(第 3 轮口径)**:`role` 的 121 **不是 bug**——role 域不走 `ScanObjsDir`,走 `NewRoleEntry`,动作目录按**职业共用**算(`1000+career*100`,代码注释已记此前踩坑修过),1111 已确认带动画渲染。**故 role 已处理**。
- **真正受「文件名启发式」坑影响的(走 ScanObjsDir 的域)= head 58 + back 12 + mount 3 + child 8 = 81 个模型**(第 3 轮说 73,漏了本轮新接的 child 8)。其余实体域(monster/npc/pet…)失配 0 → **第 4 轮的 ScanObjsDir 接入对它们是正确的**,只有 child 需等 C# 修。
- **C# 修复有现成范例**:`NewRoleEntry` 已为 role 做了「共用动作目录」解析(职业映射)。待修的 `ScanObjsDir` 应推广成读 .lh 的 clipPath(比职业公式更通用,能覆盖 head 13xx→11xx 这类非公式共用)。补丁见第 3 轮代码块。
- **已同步(commit `bef169598` `[动作解析]`)**:`export-fbx` 也传 `lh_path` → 与 `export-glb` 一致权威解析(此前漏)。至此 Python 侧 export-glb/export-fbx 均已 lh 权威;仅 `list-animations`/模型列表 anim_count 仍用启发式(低价值/热路径,暂不改)。

**第 6 轮(2026-06-19):无模型域定性 + 接入小宠/杂项 → object 域全覆盖**：
- 第 4 轮按 `res/model/` 目录判定漏掉的 4 个域,本轮定性(它们 res 布局非标=`res/{name}/model/`,但 `.lh` 的 meshPath 相对解析正常,laya_to_glb 实测均导出齐全):
  - **小宠 littlepet 23**:动作标准(`object/littlepet/action/{id}`)、**0 失配** → 本轮接入(ScanObjsDir 完全正确)。
  - **杂项 other 5**:`model_clothe_40xxxx`,4/5 共用 `action/401416` → 本轮接入,4 个待 C# 动作修复覆盖(同 child/mount)。
  - **faqi 26**:动作在 `res/model_faqi_X/action/`(非标)+ clip 名特殊(`model_clothe_240009idle`)→ **暂不接**,待 C# 改读 .lh clipPath 后再接。
  - **fashion**:无 .lh,全是 `model_clothe_XXXX_N.jpg/.ktx`(角色时装品质变体贴图)→ **非模型域,不接**。
- 已 commit `[补域]`(littlepet/other)。**至此 `object/` 下所有「标准/可标准化」模型域已全部接入 AssetHub**;仅 faqi 待 C# 动作修复后接。
- **资源域「发现+接入」阶段基本收尾**:接入 = role/创角/head/weapon/back/wing/mount + 9 实体 + littlepet/other + effect = **18 个域**;余 faqi(待 C#)、fashion(贴图非模型)。

**打法(铁律)**：每个域**先拿一个真实样本端到端验证「转换→Unity 能渲染」，再批量转**。模型上游导出器=`laya_to_glb.py`；Unity 侧导入器=`Assets/Editor/AssetHub/Laya3DImporter.cs`(模型)/`LayaEffectImporter.cs`(特效)；域开关=`AssetHubDomains.cs`。规则参考 `.cursor/rules/laya-unity-coords-and-direction.mdc`。

**自走能做 vs 需用户/环境**：
- 自走可做：只读诊断管线、改 `tools/yu-resource-tool`(Python 上游)与 `yu_client_unity`(C# 导入器/域表)代码、补缺域(如坐骑)、跑 `laya_to_glb.py` 产出 GLB 并做结构校验(用 `python/.venv`)。
- 需用户/环境(攒"待用户验收")：在 `神霄/资产管理` **批量转(Unity 操作)**、在 Unity 里**验证模型渲不渲染/对不对**、跑 Blender。

**范围(本阶段授权)**：可动 = `tools/yu-resource-tool`(在 yu_client 内，已授权改) + `yu_client_unity`；碰 yu_server / yu_gm / yu_client 其他部分 → 停下问用户。Blender 在 `D:\Blender 5.1\blender.exe`。

---
（以下为"更多协议后端"自走 backlog，资源/UI 阶段之外的兜底，沿用）
自走规则：闷头推**纯代码**项（MCP 编译验证 + 一项一 commit）；需要用户实机/视觉的攒进下面「待用户验收」，**不自己做**。每轮开局读这里取下一项。

**纯代码 backlog（安全增量优先；循环按序取，做完划掉）**：
1. ~~**G 地基 `MainUIRouter`**~~ ✅ `fc6db3c8`（按钮中央路由，open-if-ported-else-log）。
2. ~~**公告(notice) 协议后端**~~ ✅ `2ece8e63`（11020 系统公告 / 11015·11018 传闻 → `NoticeController`+`NoticeModel`，发 EVT_SYS_NOTICE/EVT_CHUANWEN）。显示 UI（跑马灯/公告面板）留待验收。
3. **更多协议后端（自走主力）**。已完成：公告 ✅、邮件 ✅ `10348895`、功能开放达成奖励 ✅ `40c22f98`、首充 ✅ `95e94651`、周卡 ✅ `d8da25d3`、惊喜礼包 ✅ `c77ecad3`（小系统策略生效）。
   - **⚠ 自走现状提示（给用户）**：现在自走轮多在刷**安全小后端**（活动/充值类），对**可玩度**提升有限。真正推进可玩度的是**渲染 D/E/F + 大模块 + 各 UI 预制体**——这些都需要你在场（实机看/调预制体）。建议你有空时来一次，集中过这批"待用户验收"项；自走继续兜底刷小后端。
   - **坑/教训①**：协议号**别按区间猜**。`pt_139`=武器养成、`pt_190`=邮件（都不是好友）。做某系统前先读其 Controller 的 `RegisterProtocal(...)` 实际号 → 再开对应 `pt_*.erl`。
   - **坑/教训②（本轮确认）**：**大模块不适合单轮自走**。好友、队伍勘察均为大模块——队伍=`240xx` **60+ 协议**、schema 复杂；好友与邮件/私信混编。单轮里逐一追踪核心协议会 rabbit-hole。
   - **自走选取规则**：先看候选系统 Controller 的 `RegisterProtocal` **数量**——**≤ ~8 个且格式简单**才在单轮做；**> ~10 个或 schema 复杂**（好友/队伍/公会/背包/聊天）→ 归下面「大模块」，不在单轮自走做。
   - 也已补：**场景对象字段广播** 12070/71/72/74 `ab0d2e62` + 12075/82/86 `b086d1b9` ✅（更新 RoleVo Hide/Ghost/Group/PkStatus/Show/Speed/名字）。仅 12084攻速/12085区域待补（需新字段）。
   - **下一项**：下一个小系统（先读 Controller 确认协议少且无重叠）。

**大模块（需专门集中处理，非单轮自走；多数 UI 还需用户在场）**：
好友（与邮件/私信混编）、队伍(`240xx` 60+)、公会、背包、聊天。这些留到能集中多步处理时做（或与用户配合）。
4. **G 按钮接线**：给已显示按钮 `UIUtil.AddClick → MainUIRouter.Open`。坑：`UIUtil.AddClick` 重复调会叠加监听、翻面换行复用同 item → 需妥善处理；改 View 低风险但点击行为要留意。
5. **渲染 D/E/F（多实体合成台 / 其他玩家 / 怪物·采集·NPC）**：要重构正在正常工作的主角合成台、且只能肉眼校准 → **归「需与用户配合实机验证」，自走轮先不碰**（可纯数值验证的骨架另议）。

**待用户验收（你在场时做，循环不碰）**：
- **【动作目录·高优】** 待 Unity MCP 在线,我把 `AssetHubDomains.ScanObjsDir` 改读 .lh clipPath(补丁已备好,见上「第 3 轮」),编译验证后 **重转 head/back/mount 三域** → 修复 ~73 个共用动作模型(head 58 / back 12 / mount 3)的「无动画」问题。重转后实机抽查 head_1301、back_1001 这类是否有动作。
- **【实体域·M1 关键】** 第 4 轮已接入怪物/NPC/宠物等 9 域(~241 模型)。**先把动作目录的 C# 修复落地再批量转**(否则共用动作的实体会无动画)。M1 优先转 **怪物(118)+NPC(35)** → 实机看场景里怪/NPC 渲染。建议每域先转 1 个样本肉眼确认再全量。
- **【坐骑域】** 在 Unity 菜单 `神霄/资产管理` 选「坐骑」域 → 批量转(38 个 model_mount_*) → **肉眼看模型渲不渲染、动作(idle/run/show/show2)对不对**。上游 GLB 全 38 已验通(0 失败)。**已知**:其中 1021/1022/1023 用 C# 当前逻辑会转成「无动画」(共用 action/1007/1008/1009,工具会告警「动作目录无 .lani」)——这 3 个等我把 C# 的动作目录解析改成读 .lh clipPath 后重转即可(待 Unity MCP 在线;算法已在 Python 验证)。若其余报错/不渲染回报现象。（注:「角色骑上坐骑」组合属 `RoleModelAssembler`,另议。）
- 公用弹层闭环 validation（验证"我出结构、你调效果、能跑"整条链）。
- 各页预制体调位置/大小/颜色/图。
- 场景渲染实机校准（合成台取景常量、多实体不串位）。
- 整体好不好玩。

## 里程碑阶梯（每级用户可在 Editor Play 验收）
- **M0 地基管线**：SOP + 进度文档 ✅；UICreator 闭环验证（公用弹层）⏳。
- **M1 进城可动**：登录 → 进游戏 → 见自己角色、能走、见其他玩家/NPC/怪物/采集物。
  - 后端：协议 + 数据层 ✅（见下「已完成」A/B/C）；渲染 D/E/F ⏳（需实体模型资源 + 实机校准）。
- **M2 主界面 + 交互**：主 HUD 按钮中央路由、公告、公用弹层接入；点 NPC/怪有反馈。
- **M3 战斗**：打怪 / 技能 / 死亡复活（最难，强依赖资源）。
- **M4+ 模块长尾**：背包 / 角色 / 装备 / 公会 / 婚姻 / 副本 / Boss / 活动 …

## 已完成（检查点）
| 提交 | 内容 |
|---|---|
| 756023ab | 场景 12001 移动广播接收 + MoveType |
| 3bf85b99 | SceneManager + RoleVo/MonsterVo/NpcVo/DropVo 数据层（字段对照 pt_120/121） |
| 9b5afc2f | 12002 真解析 + 全场景增删改协议接入 SceneManager |
| 更早 | 登录模块全链路、场景地图滚动复用、主角建模/移动/相机跟随/2.5D 倾斜、镜头大小校准 |

## 状态表
| 模块 / 页 | 状态 | 备注 |
|---|---|---|
| 登录 LoginModule | ✅ 链路通 | UI 效果待用户验收 |
| 场景协议 + 数据后端 | ✅ | A/B/C + 字段广播 12070/71/72/74/75/82/86；仅 12084攻速/12085区域待补(需新字段) |
| 场景渲染（其他玩家/怪/NPC/采集） | ⏳ 待做 | D/E/F：多实体合成台 + 需实体模型资源 + 实机校准 |
| 公用弹层 | ⏳ 待验收 | 验证 UICreator 闭环（需用户在场） |
| 主界面按钮 | ⏳ 路由地基已建 | `MainUIRouter` ✅(fc6db3c8)；按钮 AddClick 接线待做(backlog 4) |
| 公告 notice | ✅ 后端完成 | `2ece8e63` NoticeController(11020/11015/11018)；跑马灯/面板 UI 待验收 |
| 邮件 Mail | ✅ 后端完成 | `10348895` MailController(19001/19007/19008/19009)；详情/领取待续、列表UI待验收 |
| 功能开放达成奖励 | ✅ 后端完成 | `40c22f98` FunctionOpenController(13800/13801/13802)；红点/领取 UI 待验收 |
| 首充 FirstRecharge | ✅ 后端完成 | `95e94651` FirstRechargeController(15905/15906/15908)；面板UI待验收 |
| 周卡 WeekCard | ✅ 后端完成 | `d8da25d3` WeekCardController(45201/45202/45203)；面板UI待验收 |
| 惊喜礼包 SurpriseGift | ✅ 后端完成 | `c77ecad3` SurpriseGiftController(49000-49004)；面板UI待验收 |
| 好友/队伍/公会/背包/聊天 | ⏸ 大模块·延后 | 协议多(队伍240xx 60+)/schema 复杂；需专门集中处理(非单轮自走)，UI 需用户 |
| 战斗 / 技能 | ⬜ 未开始 | 最难，靠后 |

> 状态图例：✅ 完成 / ⏳ 进行中 / ⬜ 未开始 / ⛔ 卡住（注明缺资源 or 缺逻辑）

## 关键事实速查（少走弯路）
- UICreator：菜单 `神霄/LayaUI` → `LayaUIPipeline.RunModule("{module}")`；验收后勿重转（覆盖手调）。
- 模块模板：`Module/Core/Login`（Bootstrap + Flow + Controller + Model + Views:Bind + 一个模块预制体）。
- 坐标/方向/2.5D：见 yu_client `.cursor/rules/laya-unity-coords-and-direction.mdc`（Unity `MODEL_TILT=-38` 等）。
- 协议真相源：yu_server `src/pt/pt_*.erl`；老客户端行为参考：yu_client `h5/src`。
