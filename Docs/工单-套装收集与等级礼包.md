# 工单:套装收集(Suit_clt)+ 等级礼包(Award_lv_gift)最小闭环

日期:2026-07-02(第16轮侦察代理产出,主控审定)
目标:解开主线卡点 #42(task 100391「激活二忍套装4件」)与 #46(task 100420「领取35级冲级豪礼」)。

## 任务条件(server data_task.erl 权威)

- **100391**:ctype=84, id=1, need_num=4 → **套装1(suit_id=1)的 cur_stage 达到 4** 即完成
  (服务端 lib_task.erl:1652 按 {suit_clt, [{SuitId,CurStage}]} 匹配;任一阶段激活都触发 lib_task_api:suit_ctl)。
- **100420**:ctype=54, id=35, need_num=1 → **领取过 35 级冲级礼包**(rush_bag_lv_list 含 35)即完成
  (服务端 pp_welfare.erl:121 领取成功后 lib_task_api:award_lv_gift(PS, BagLv))。

## 系统 A:套装收集(协议段 pt_152 内嵌 15256-15259)

### 协议(ClientProtocol.json:769-788;c=u8/h=u16/i=u32/l=u64,大端)
- **15256** 请求无参(老端 GAME_START 主动发);回包:
  `clt_list[u16×{suit_id:c, cur_stage:c, cur_pos_list[u16×{equip_type:c}]}] + suit_id:c`(末尾 suit_id=当前穿戴时装)
- **15257** 激活套装阶段,发 "cc"(suit_id, stage);回包:`code:i, suit_id:c, cur_stage:c, cur_pos_list[u16×{equip_type:c}]`
  ——**任务闭环唯一触发点**;code==1 成功,否则错误码(显码降级)。
- 15258(穿装自动点亮广播:`suit_id_list[u16×{suit_id:c}] + equip_type:c`)本轮可接可不接(接了只落数据)。
- 15259(时装穿脱)留后。

### 老端锚点
- Model:h5/src/commonModel/SuitActivityModel.ts(SetSuitData=15256 :70-87 / AddSuitData=15257 :90-100 / GetCurStage :293)
- Controller:h5/src/commonController/SuitActivityController.ts(注册 :116-119;发包 :122-130 `SendFmtToGame(15257,"cc",suit_id,stage)`;GAME_START 发 15256 :180-183)
- DoTask case Suit_clt(TaskModel.ts:1878-1884):is_skip → `OpenFunHandler(196)` = 打开套装收集主界面(显示名「万古帝胄」)。

### 配表(⚠不在 config_table_default 列序表内,按 JSON 具名 key 直读)
- `config_suit_clt.json` 主键 `{suit_id}@{career}`:suit_id/career/name/open_lv/open_turn/show_equip/stage/star/color…
- `config_suit_clt_process.json` 主键 `{suit_id}@{suit_stage}`:attr/skill_id(展示用,可后)
- `config_suit_clt_task.json` 可选。

### Unity 最小闭环
- SuitCollectModel(clt_list 落库 + GetCurStage)、SuitCollectController(GameStart 发 15256;Activate(suitId,stage) 发 15257 "cc";On15256/On15257)、TEMP 壳(套装1名称+cur_stage/4 进度+「激活下一阶」按钮=cur_stage+1)。
- 同步 config_suit_clt(+process 可选)。
- ⚠服务端激活需背包有对应部位装备(lib_suit_collect.erl:249-273 前4套特殊分支自动从背包换装)——活服联调时测试号背包需有装备,壳上失败显码即可。
- DoTask ctype 84 → 开 TEMP 壳(替换降级映射)。

## 系统 B:等级礼包(协议段 pt_417;服务端=冲级豪礼 rush_giftbag,勿与 lib_custom_act_lv_gift 33248/33249 混淆)

### 协议(ClientProtocol.json:4114-4118)
- **41700** 请求无参(老端 GAME_START 发);回包:`giftbag_state[u16×{lv:h, received:c, end_time:l, remain_num:i}]`
  (received:0 未达到/1 可领/2 已领/4 已被领完;end_time l=64位)
- **41701** 领取,发 "h"(lv);回包:`lv:h, code:i, rewads:ObjectList`——**任务闭环唯一触发点**;
  code==1 成功后老端再发一次 41700 刷新。rewads 的 ObjectList 读法参考既有 ObjectList 解析(若 Unity 无现成解析,按序读完剩余字节并日志 remaining,奖励展示走 toast 降级)。
- 41717(全服剩余广播)老端标注没用,跳过(若服务端推送需按 `lv:h, remain_num:i` 读掉避免错位——建议注册空读)。

### 老端锚点
- WelfareController.ts:注册 :374-375;Handler41700 :66-78(落 WelfareModel);Handler41701 :80-88(code==1 → 刷 41700 + 弹奖励;else 错误码);GAME_START 请求 :445。
- 领取动作:levelReward/LevelRewardItem.ts:99 `Fire(...,41701,"h",lv)`;:106-112 若当前主线任务==100420 领取后自动关窗。
- DoTask case Award_lv_gift(TaskModel.ts:1467-1489):OPEN_ACT_VIEW("331@3",417) 开活动界面;
  另有 IsAllStepFinish 早退分支(:783-792)取消 KaiFuBtn 高亮——Unity 现有 DoTask 已有完成早退,无需改。

### 配表(⚠不在列序表内,具名 key 直读)
- `config_rush_giftbag.json` 主键=bag_lv 字符串:bag_lv/bag_name/bag_upperlimit/bag_upperday/bag_gift_man/bag_gift_woman…

### Unity 最小闭环
- RushGiftModel(giftbag_state 落库)、RushGiftController(GameStart 发 41700;Receive(lv) 发 41701 "h";On41700/On41701:code==1 → 再发 41700 + toast「领取成功」;else 显码)、TEMP 壳(列表逐 lv 一行:名称+received 状态文案+可领时「领取」按钮;对照 received 0条件不足/1可领/2已领/4已被领完)。
- 同步 config_rush_giftbag。
- 领取成功且当前主线任务为 100420 → Close 壳(对标老端)。
- DoTask ctype 54 → 开 TEMP 壳。

## 共享接线(主控做,代理勿碰)
Proto.cs 常量、GlobalEvent 事件、ControllerHub 注册、TaskModel.DoTask 分支与 UNPORTED_TIP_SYSTEM 移除。

## 红线
- 不造假数据;错误码表未移植统一「失败(code)」显码;TEMP 壳样式从简;协议字段序逐字段照本工单;
- 实现代理只建各自模块目录新文件 + CliVerify 用例,不碰共享文件、不跑 Unity、交付前 dotnet build 0 错。
