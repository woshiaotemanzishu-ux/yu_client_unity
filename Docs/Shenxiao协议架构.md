# 协议架构(yu_server ⇄ 客户端,2026-06-12 查证)

进入游戏前的准备文档:帧格式、协议分段、路由、进游戏推送链、心跳/重连约束,
以及 Unity 侧的承接设计。所有事实均出自 yu_server / yu_client 源码(标注文件位置)。

## 1. 帧格式(已在 Unity 实现并跑通)

- 收发均大端序(BIG_ENDIAN)。
- **服务端 → 客户端**:`[u32 总长][u16 cmd][u8 压缩位][payload]`,总长 = payload + 7;
  压缩位恒 0(pt.erl:360 压缩逻辑被注释)。
- **客户端 → 服务端**:`[u32 总长][u16 cmd][u8 压缩位=0][payload]`(UserMsgAdapter.ts:491)。
- 基础类型:c=u8 / C=i8 / h=u16 / H=i16 / i=u32 / I=i32 / l=u64 / L=i64 /
  s=u16 长度+UTF8;数组=u16 计数+元素;浮点=整数×1000(pt:read_float,3 位精度);
  语音=u32 长度+bytes。figure 复合块见 `FigureProto.cs`(已对齐 pt:write_figure)。
- Unity 对应物:`NetManager`(分帧/收发/心跳)、`NetReader`(顺序读+ReadFmt)、
  `UserMsgAdapter.cs`(帧编码)、`BaseController`(注册回调)。

## 2. 协议号分段(yu_server src/pt/pt_*.erl)

路由:`mod_server.erl:635 routing/3` 取协议号前三位(百位)分发到 pp_* 模块。
**协议号体系不可改**,Unity 必须原样使用。

| 段 | pt 文件 | 模块 | 段 | pt 文件 | 模块 |
|---|---|---|---|---|---|
| 100xx | pt_100 | 注册登录(已接) | 134xx | pt_134 | 勋章 |
| 102xx | pt_102 | 游戏控制 | 135xx | pt_135 | 九魂圣殿 |
| 110xx | pt_110 | 聊天 | 137xx | pt_137 | 钻石大战 |
| 111xx | pt_111 | GM 秘籍(已接) | 138xx | pt_138 | 模块预告 |
| 112xx | pt_112 | 装扮 | 139xx | pt_139 | 好友 |
| 113xx | pt_113 | 微信 | 140xx | pt_140 | 战斗相关 |
| 120xx | pt_120 | 场景信息 | 141xx | pt_141 | 关系 |
| 121xx | pt_121 | NPC | 142xx | pt_142 | 伙伴 |
| 130xx | pt_130 | 玩家信息 | 150xx | pt_150 | 物品 |
| 131xx | pt_131 | (未明) | 151xx | pt_151 | 交易市场 |
| 132xx | pt_132 | 离线挂机 | 152xx | pt_152 | 装备 |
| 133xx | pt_133 | 结界守护 | 153xx | pt_153 | 商城 |
| | | | 200xx | pt_200 | 战斗信息 |
| | | | 300xx | pt_300 | 任务 |

(40000+ 公会、16000+ 技能等更多段在 src/pt 全列,接对应模块时再查。)

## 3. 登录段(100xx)已接协议与勘误

| cmd | 方向/格式 | 说明 |
|---|---|---|
| 10000 | 发 "iiss" / 回 "clihi"+角色 | 账号登录。**accname/time/pid 用 get_server_info 下发值** |
| 10003 | 发 "cccsslsscscc" / 回 "cl" | 创角 |
| 10004 | 发 "lsisisscscsh" / 回 "c" | 选角进游戏(成功=1 → GAME_START) |
| 10006 | 空 | 心跳。**服务端有频率限制(pp_login.erl:219,登录后计数防刷)** |
| 10007 | 发 "s" / 回 "c" | **角色名验证**(勘误:曾误标为踢线通知,已改 Proto.NAME_VERIFY) |

## 4. 进入游戏后服务端推什么(mod_login.erl:690+)

10004 回 1 之后,服务端起玩家进程并触发 `?EVENT_LOGIN_CAST`,各 lib_* 模块**主动推送**初始化数据,主要有:

- 130xx 玩家:13001 属性 / 13002 经验 / 13003 等级 / 13006 金币 / 13011 世界等级 /
  13017 托管 / 13080+ 头像……
- 150xx 物品(lib_goods:login)、16000+ 技能、40000+ 公会;120xx 场景与出生点。
- 老客户端 GAME_START 后也会主动**请求**一批(任务 30005、龙珠 143xx 等,各 Controller 自发)。

**Unity 承接设计(已定,按需逐模块实现):**

1. 一个业务模块 = 一个 `BaseController` 子类(单例 + `Register()` 注册本段协议),对标老客户端
   commonController/*.ts 一比一搬;`GmCheatController` 是首个游戏内模块样板。
2. 未注册协议:NetManager 收到没有 handler 的 cmd 时只记 Debug 级日志不报错——进游戏初期
   服务端会推几十条我们尚未实现的协议,这是预期内噪音,按模块推进逐个消化。
3. 进游戏后第一个落地目标:RoleController(13001/13002/13003/13006)+ 场景段(120xx 出生场景),
   即"进入游戏能看到主角"。
4. 复杂回包优先在 pt_*.erl 里查 write 格式(服务端是格式真相源),old client ReadFmt 做对照。

## 5. 心跳 / 断线 / 顶号(约束清单)

- 心跳 10006:我们按 AppConfig.heartbeatIntervalSec 定时发(NetManager.ConfigureHeartbeat),
  服务端登录后有防刷计数(pp_login.erl:219-230),间隔别低于秒级,当前配置安全。
- 老客户端重连状态机(UserMsgAdapter.ts:183-290):4 类 reconnect_type、最多 4 次提示、
  顶号(other_place_login)禁止自动重连。Unity 暂未实现重连(断线即回登录),
  做主城线时按此状态机补——记账。
- 踢线/顶号的真实协议号待查(不是 10007),接聊天/登录段细化时确认。

## 6. GM 秘籍(111xx,已接)

- 11100 请求清单(空)→ 回包 u16 分类数 × { s 分类名, u16 命令数 × { s 命令, s 中文名,
  u16×s 参数描述, u16×s 默认值 } }(pt_111.erl)。服务端 pp_gm.erl 约 350+ 条命令,
  **清单由服务端下发,客户端零硬编码**。
- 11101 执行:发 "s",格式 `命令_参数_参数`(下划线分隔,pp_gm.erl:737 string:tokens)。
- 鉴权:`config:get_gm_password()` 为空 → 全放行(开发模式);非空 → 先发
  `setgmpassword_密码`(进程内记住)。无等级/账号位判断。
- Unity 工具:`神霄/GM 秘籍`(Play 模式)——拉取清单 → 分类/搜索/参数默认值/一键发送,
  顶部直发框可敲任意命令。运行时 `GmCheatController` 可被其他调试入口复用。
- 老客户端对照:按 Z 呼出 CheatInputView(KeyInput.ts:65,需 TestYouWant=1 或本地模式)。


## 7. 进游戏框架(Phase A 落地,2026-06-15)

进游戏后的协议接入骨架就位,"进游戏看到主角数据"打通:

- **ControllerHub**(Module/Core/Game):游戏内控制器注册中心。进游戏 `InitAll()`、断线/登出 `DisposeAll()`;新增模块在 `ALL[]` 加一行。当前含 RoleController、GmCheatController。
- **GameEntryFlow**(Module/Core/Game):进游戏编排。`EVT_GAME_ENTERED`(10004 成功)→ 注册控制器 → 等 13001 全量 → 发 `EVT_ROLE_READY`(交主城/场景);`EVT_NET_DISCONNECTED` → 注销 + 重置主角。`[RuntimeInitializeOnLoadMethod]` 自装,无需手挂。
- **RoleController + RoleModel**(Module/Core/Role):首个游戏内模块。接 13001(全量)/13002(经验)/13003(升级 hll)/13006(货币 liii),写 RoleModel,发 `EVT_ROLE_INFO_UPDATE`。格式严格对标 pt_130。
- **BaseController 升级**:记录已注册协议号,`Dispose()` 统一注销(可重入)。
- **NetReader.ReadArray<T>**:u16 计数数组通用读取(对标 pt:write_array);**BattleAttrProto**(Common/Proto):Hp/HpLim/Speed + 属性表,对标 pt:write_battle_attr。

**模块接入模板(后续每个 130xx+ 模块照此)**:
1. `Proto` 加该段协议号常量(注释带格式串 + pt 出处)。
2. 复合 VO 放 `Common/Proto`(跨模块)或模块内;数组用 `NetReader.ReadArray`。
3. `{X}Controller : BaseController`,Register 注册回调,回包写 `{X}Model`,发 `EVT_*`。
4. 在 `ControllerHub.ALL` 注册一行。

**下一步**:① 真机验证 13001 链路(进游戏 Console 看到主角名/等级/货币 + 不刷未注册错误);② 场景段 120xx 最小接入(出生场景);③ Phase B 协议代码生成器(schema→常量/VO/读取/桩)。

### 7.1 全局错误 10205 负约束纠偏（2026-08-02 核对）

- `10205` 的双端事实仍是无C2S、S2C `error_code:u32,args:string`，服务端全部 `lib_game:send_error/2..4` 最终写入本号，老端 `ServerTimeController` 会无条件显错；这些证据只说明协议活跃，不等于改写产品边界。
- KfStage权威段在R111已明确“不接10204/10205/10208/10209”，而R484的10205常量、`GameStartController`常驻注册、Toast/日志handler和正向用例均为其后的晚增量，且没有同步改写原禁令。R541按负约束优先规则全部撤销；运行时不得注册10205，也不得另建no-op/raw模型或自动请求。
- 10204继续按老端无sender维持KILL；10207已由R521随CDN正文、登录弹出、角色隔离红点和真实页面完整落地；10211继续等待`data_popup`配置、登录/定时/神殿条件与实际弹窗消费者闭环。

#### 7.1.1 历史深海订阅 10208/10209 死链（R544）

- 10208的历史C2S是严格空查询，S2C仅`subscribe_type:u8`；老端现仓只剩`proto102.d.ts`和`ClientProtocol.json`契约，`h5/src`以及开发/发布两份`bundle.js`均无注册、请求或消费者。
- 当前服务器`pp_game.erl`中10208查询handler与紧随的唯一`pt_102:write(10208,...)`调用整段注释；10209写入handler也在同一注释段内，且没有10209 S2C writer。`pt_102`的read/write定义只是不可达codec，不能作为接入依据。
- 10208正式KILL：Unity不得注册、请求、加入GAME_START或建立raw/UI/事件切片。11307微信订阅总开关和11306模板状态走独立`pp_wx`路由，禁止与10208/10209互相别名；只有服务器恢复真实handler且产品恢复对应消费者时才重审。

### 7.2 时空圣痕 204xx 只读切片（2026-07-30 核对）

- GAME_START 仍只空发 `20411`；`20401/20404/20405/20407/20409/20410` 仅允许业务显式空查，`20402` 显式发送 `castle_id:u16` 且兼容同号服务端推送。收到 `20411 status==1` 或等级变化都不得自动扇出其他 204xx。
- `20401` 保存个人/本服争夺值和完整有序据点表；`20402` 按回包 `castle_id` 只替换独立明细桶。两者即使字段形状相同也不共写，避免明细晚包污染主快照。
- `20404` 每日来源表、`20405` 每日阶段奖励状态、`20407` 赛季目标、`20409` 个人排行、`20410` 当前据点和 `20411` 世界服表均为独立原始切片。列表保持 wire 顺序与重复项，空表是 loaded 清旧；零值、u32/u64 最大位型不作“未加载”推断。
- 读侧不排序、不加载配置、不发事件或红点，也不展示或发放奖励。`20403` 驻扎/传送、`20406/20408` 领奖及其 `20400` 错误链仍须随对应 UI/场景/奖励闭环迁移，当前禁止公开 sender 或孤立结果处理。

### 7.3 幻兽 173xx 读侧切片（2026-07-30 核对）

- GAME_START 仍只空发 `17301`。17302无C2S，是服务端在装备、状态或强化变化后推送的完整单兽条目；它只替换已加载17301列表中的首个同ID项，早包与未知ID不补项，且不修改FightCount。
- `17308` 与 `17309` 只允许业务显式查询。17308请求携带目标goods u64、double标记和u16计数的材料goods u64表，回包完整替换最后一次强化预览；17309请求携带module/sub和属性表，回包按module/sub复合键替换战力。请求无回复时都保留旧值，回包中的零值和u64最大位型均为有效原始数据。
- 17300错误、17301总览、17308预览、17309键控战力彼此隔离；17301新快照不会顺带清预览、战力或错误。读侧不接配置、背包、事件、红点、UI、3D或本地战力推导。
- `17303-17307/17310-17312` 会真实装备、脱装、上下阵、扩位、消耗材料强化或合成。它们必须等背包/GoodsModel、配置、场景表现和结果消费闭环一起迁移，当前禁止公开sender或只接成功回执。

### 7.4 至尊VIP 451xx 的全量、通知与商城隔离（2026-07-30 核对）

- GAME_START固定空发 `45101 -> 45102 -> 45104`；45101承载基础信息和完整权益表，45102承载阶段/子阶段及技能任务全量，45104承载至尊币任务全量。三份列表均保留wire顺序与重复项，空表是loaded清旧；角色等级或vip_flag变化只复判入口，不再借事件重拉协议。
- `45109` 是永久VIP升级空通知，只重查45101；`45110/45111` 是技能/至尊币任务的S2C变化子集，先保存独立最后通知，再分别重查45102/45104。通知本身不就地patch全量，避免乱序晚包制造混合快照。
- `45112 is_free:u8` 独立覆盖，Unity遵循老端只收不发，不与45101尾部免费保护字段共写。45120 SVIP活动继续由独立控制器持有；商城15301的type10商品只落商城专槽，绝不能转发或伪造成45102任务包。
- 45103/05领奖、45106/07购买、45108权益领奖属于写事务，当前随UI、配置、背包/货币和奖励闭环整体延期。读侧不接红点、弹窗、自动购买或本地发奖。

### 7.5 神庭 233xx 的全量、keyed推送与490级补拉（2026-07-30 核对）
- `GodCourtController` 在GAME_START先清全部神庭状态，再严格发送两个6字节空帧 `23301 -> 23306`。角色信息事件只在等级真变且新等级精确为490时补发相同序列；从489跳到491不会补拉，对齐老端的精确等级判断。
- `23301` 是包含属性、装备和套装嵌套表的完整神庭有序快照；所有层级保留wire顺序、重复ID和u64装备/战力位型，外层空表是loaded清旧。`23306` 是独立水晶屋全量，保留大奖状态表的原序、重复次数和raw状态，空表只清本切片。显式查询没有回包时均保留旧值。
- `23310` 是与23301单个Court同结构的S2C-only完整更新。由于协议没有序列号且当前没有UI消费者，它按court_id落入独立keyed覆盖表：早包有效、同键替换、异键共存，不就地patch 23301，避免晚到总览与推送合成无权威顺序的混合快照。`23300`错误码/参数同样是独立raw覆盖。
- 当前只建立读侧数据底座；23302-05/23307-09的解锁、穿戴、升阶、强化、开水晶和领奖均延期到背包/装备、配置、货币、奖励、UI闭环齐备后迁移，不接入口、提示、事件、红点或本地推导。

### 7.6 VIP 450xx 的启动快照与通知隔离（2026-07-30 核对）

- `VipController.RequestStartup` 先清整个VIP模型，再严格发送三个6字节空帧 `45000 -> 45004 -> 15800`。跨天重拉同一安全读侧子序列，但不预清已有快照，避免无回包时丢状态；15901福利卡与15803累计充值都只允许显式查询，不得借启动/跨天恢复旧端额外请求。
- `45000` 完整保存VIP等级、经验、下一档经验、隐藏态、已领/可领奖励有序表和免费卡有序表；`45004` 独立保存特权卡类型、体验/激活/永久raw标志与时间。所有表保留wire顺序和重复ID，零值/u32最大位型有效，空表是loaded清旧。
- `45005` 激活与 `45006` 超时只保存各自最后一份双u8原始通知，绝不直接改45004。45006收到后严格重拉一次45004，让服务端全量成为唯一卡表权威；45000、45004、45005、45006、15800/15801、15802/15803与15901切片互不交叉清理。
- 15802是S2C-only严格空体充值成功通知；服务器在后台充值、物品充值和直购币入账链都会发送。每包都标记通知loaded、显示一次成功提示并发一次无参事件，但不据此本地加钻石、补商品或自动重查。15803显式空查询及服务端主动推送共用 `total_gold:u32` 绝对快照，0和最大值都有效，每包替换并发携值事件；无回复保持旧值。
- 15804会校验商品、扣除直购币、派发礼包并触发充值事件；45001/02领奖、45003购买、45007领免费卡和45008隐藏VIP同样会写资产或角色状态。它们必须随UI、配置、支付确认、背包货币、single-flight、奖励和提示闭环整体迁移；当前不改Role/Figure，不接红点、倒计时、自动购买或本地奖励。

### 7.7 勋章/称号 134xx 的只读边界与写事务隔离（2026-07-30 核对）

- GAME_START只发送 `13401 -> 13405` 两个严格空帧。13401完整覆盖勋章ID、强化等级/经验、u64声望、原始战力和符文副本层数；13405完整覆盖所有称号条目，包含未激活的level=0项，保留wire顺序，空表loaded清旧。13400只保存独立raw错误码，不对三份状态做交叉清理。
- 13402晋升不是查询：它扣配置物品、改勋章与Figure/Scene、重算属性、落DB，并串接任务、成就、推送礼包、神殿觉醒、活动及至尊VIP。13403激活/升星同样扣物、落DB和重算属性，首次激活还自动佩戴并广播；13404/13406会持久化佩戴态并同步Role/Figure/Scene。
- 13407请求体是背包实例维度的 `u16×{goods_auto_id:u64,num:u32}`，服务端直接删减物品、写强化等级/经验、重算属性并同步场景战斗属性。13402/03/04/06/07必须等配置、背包实例、角色与场景同步、错误/成功提示、UI和权威重拉闭环齐备后整体迁移，禁止仅接结果包或暴露裸sender。

### 7.8 远古奥术 211xx 的不可达边界（2026-07-30 核对）

- 21101的wire是 `core_type:u8,arcana_list:u16×{id:u32,lv:u16,break_lv:u16}`，服务端查询路径仍能回包；但老端GAME_START空发后，接收函数只取SCMD就结束，没有保存、事件或展示。技能页第四个“远古奥术”标签及ForbiddenSkillView槽位已注释，`SkillUIModel` 的奥术配置、数据和成功更新逻辑也整段注释，因此该启动请求只是历史空耗。
- 21102升级、21103突破、21104选择核心在服务端仍是真实写事务，分别可能扣物、写DB、改变技能/快捷栏/属性/场景或持久化核心选择；然而老端全仓没有三号的实际发送点，只剩通用serializer与结果Toast。服务端模块的`@deprecated`不能当作死服务证据，但客户端玩家入口不可达是本项目的行为事实源。
- 四号已逐号进入killlist。Unity不得定义211常量、注册handler、恢复GAME_START 21101、建立无消费者快照或从残留回调反造sender/UI；若未来产品明确恢复远古奥术，须按新功能重新授权并整体迁移配置、界面、背包扣物、技能/快捷栏、角色属性和场景同步闭环。

### 7.9 幻饰 149xx 的启动、预览与写事务隔离（2026-07-30 核对）

- `UnrealController.RequestStartup` 先清全部幻饰原始切片，再依次发送14904的cell=1、2、3、4、5、6六个7字节帧，最后发送14908的6字节空帧。14904保存原始res、回包cell、强化等级和评分，按cell键控；14908保存完整有序解锁槽位表并接受装备/进阶后的同号主动推送。列表不排序不去重，空表是loaded清旧。
- 14906进阶属性预览和14907分解属性预览只允许业务显式携 `goods_id:u64` 查询，分别按回包goods id保存独立评分/属性表。属性的color/type/attr/interval/unit均按wire原宽度保存，重复项、空表、零值和最大位型有效；请求无回复保留旧值。两类预览互不覆盖，也不改14904/14908。
- 14900只保存独立raw错误码和参数字符串。服务端14906在没有下一阶时可能直接skip，14907有两处以单参数调用要求双字段的14900 writer而落cmd0兜底，故客户端不得把无回包解释为成功/清空或自动重试。
- 14901穿戴、14902卸下、14903进阶、14905强化均是真实装备/背包/DB/属性事务，并会联动解锁槽、至尊VIP或神殿。四号必须随幻饰UI、配置、背包同步、角色属性、成功/错误展示与权威刷新整体迁移；读侧不得暴露这些sender或本地乐观改装。

### 7.10 聊天 110xx 的 GAME_START 与缓存语义（2026-07-28 核对）

- `ChatController` 在 `GAME_START` 依次请求 `11010(仙宗) -> 11010(私聊) -> 11010(世界)`；非开服第 1 天
  再请求 `11010(小跨服17) -> 11010(百煞冲霄20)`，随后空发 `11050 -> 11064 -> 11023`，最后插入本地欢迎语。
- `11010` 不携带独立频道尾字段，空缓存只能得到 `count=0`，因此不能从空回包反推出请求频道或借此清理其他频道。
- 服务端缓存按“新 → 旧”下发；客户端写入展示模型前必须逆序为“旧 → 新”。私聊项必须保留
  `player_list` 中发、收双方，并依据 `is_read` 恢复未读状态。
- 老端把频道 20 的 `11001` 实时消息和 `11010` 缓存统一映射到小跨服频道 17；Unity 必须保持一致。
- 主界面消费链和显示规则见 [主界面聊天消息链路-经验与排障](主界面聊天消息链路-经验与排障.md)。

### 7.11 结界守护 133xx 的启动读侧与写事务隔离（2026-07-30 核对）

- `AutoBrushController` 收到 GAME_START 后先清全部模块模型，再严格空发 `13300→13301→13309→13323→13324`；后续号不得改序、条件跳过或替代既有刷怪/排行请求。
- 13309 保存原始 `code:u32,next_stage_reward_gate:u64`，gate=0 仍是 raw 0，不在数据层复刻旧 UI 的 99999 展示哨兵；13323 保存 `node:u8`；13324 原子保存 `daily_ask_time:u16,next_ask_time:u32`，并允许服务器在公会协助状态变化后主动推送覆盖。三份 loaded 切片及既有刷怪/排行/战斗状态互相隔离，Reset/Dispose 全清。
- 13310 是 `gate:u64` 阶段领奖事务，会真实发奖、写 DB 并重查 13309；13322 是 `node:u8` 新手流程持久化写入。两号必须随完整 UI、配置门禁、single-flight、奖励/背包邮件或教程状态机整体迁移，当前禁止公开 sender、孤立 ACK 或乐观状态。

### 7.12 周本 50805 的专属结算切片（2026-07-30 核对）

- 50805由周本成功/失败事件主动推送，Unity只注册接收器，不提供请求方法、不加入GAME_START或场景轮询。它与61003通用副本结算不是同一数据线，禁止复用或互相覆盖。
- 每包按 `result_type:u8,dun_id:u32,go_time:u32,dun_rewards:u16×{type:u8,times:u16,rewards:ObjectList},role_boss_list:u16×{boss_id:u32,reward_st:u8,reward_list:ObjectList}` 完整替换最后loaded快照。所有列表保留wire原序和重复ID，空表清旧，零值和u32最大值有效。
- 50801周本信息、50802榜单和50805结算普通包互不清理；只有模块断线/注销的 `PolarModel.Clear()` 统一清空。raw层不得据result_type推场景退出、打开结算页、展示/发放奖励、补查50801/02或本地修改背包。

### 7.13 协议覆盖 baseline 的证据收口规则（2026-07-30 核对）

- `baseline.json` 的 `unityRegistered/liveGap` 是冻结比较基线，不能为了显示当前数字逐轮重写；`status` 则是人工策展门禁。当前扫描中某族的全部 raw 活缺口都已被运行时注册或带非空 evidence 的 killlist 覆盖时，必须把该族转为 `done`，让断言C持续阻止新缺口；继续保留 `pending/legacy_unverified` 会让真实完工家族逃逸C段检查。
- R514按最新运行时报告与killlist交集一次收口 `110/151/211/221/240/339/399/460/470/510` 十族。状态上调不增加注册、不改变raw liveGap或对外有效覆盖率；每族 `statusNote` 必须写明具体剩余号和证据边界。以后若协议重新变活，应先更新双端证据和实现/killlist，再由C段决定是否允许done。
- 187日常海域不能用baseline收口替代实现：18705/06/07/13都是玩家可达的真实写事务，18702/08/09是场景操作且没有同号S2C handler。它们必须随场景session、配置、资产/任务和结果闭环整体迁移；禁止把写操作加入killlist，或注册不存在的回执来清数字。

### 7.14 Demon 18315/18317 接收侧边界（2026-07-30 核对）

- 18315的S2C只有 `open_state:u8`，既会由每日商店重置链主动推送，也可由C2S空请求触发；但该请求先把服务端每日计数写为1，并非纯查询。Unity只注册接收器、保存独立loaded标量，禁止公开sender或加入GAME_START。任意0～255都按raw完整覆盖，不从值派生黑市红点或“首次打开”行为。
- 18317是服务端完成生活技能进度、使魔属性和玩家战力更新后主动发送的 `demons_id:u32,skill_id:u32,skill_lv:u16,process:u32,is_active:u8`。Unity按 `(demons_id,skill_id)` 复合键完整upsert最后raw项，保留零值和全位边界；早包/未知键也可落地。它不反写18301实体树，避免把独立推送伪装成完整实体权威快照。
- 两切片只由 `DemonModel.Reset/Controller.Dispose` 统一清空，普通18301/03/07/11/14/50901包互不清理。不接旧端配置回调、技能/商店红点、事件、属性重算或UI，也不恢复18315写每日计数、18316激活技能及其余使魔资产操作。

### 7.15 Goods 15014/15015 战力分项查询（2026-07-30 核对）

- 同服显式请求15014只写 `role_id:u64`；跨服显式请求15015写 `server_id:u16,role_id:u64`。两条服务器成功链都统一下发15014：`server_id:u16,role_id:u64,other_power:u16×u32,self_power:u16×u32`。客户端以回包键为准，按服务器/角色复合键完整替换不可变快照；两表保留wire原序和重复值，空表是loaded，请求无回复不清旧。
- 唯一公开入口位于现有他人资料卡，先拦截非正角色号、自己和越界服务器号；不加入GAME_START、不自动轮询。资料卡缓存命中直接渲染，未命中才请求，事件消费必须再次核对当前服务器/角色和“战力对比”页状态，避免乱序包串页。
- 当前仓库没有旧端 `CLIENTFIGHTCOM` 分项名称/归类的权威配置，表现层只能以wire位置并排展示我方/对方原始数值；禁止猜名称、分组、相加、补齐缺项或把分项和冒充总战力。15023继续按老端无触发且服务端恒失败治理。R569确认15011与现役19501→19502都从同一 `GoodsStatus/lib_goods_util` 权威源构造相同九字段装备元组，而19502另含法阵/仙灵并已有同服/跨服入口、页面消费者及回归，故15011按“现役产品链已替代”进入killlist，禁止形成第二份资料快照。15028是会真实扣料、产物并回奖励的写事务，进入hard-negative；只有配置、材料冻结、背包权威、单飞、错误及奖励表现一起闭环时才重审。

### 7.16 运营公告 10207 的协议/CDN双段链（2026-07-30 核对）

- 服务器wire只有S2C `type:u8`，PHP/GM通过 `notify_handle` 广播；Unity无请求方法、不加入GAME_START。每包保存最后raw type，0只记录，非0进入与登录页初查共用的串行CDN刷新，避免并发版本/正文交错提交。
- 版本与正文分别来自平台 `url_cdn_path/login_notice/jzy/login_notice.json.cfg.v` 和 `.json.cfg`。正文必须同时具备 `belong`、`notice` 对象，成功后才原子替换并提交版本；网络失败、版本JSON或正文JSON/结构失败都保留旧快照。冷启动内存无正文时，即使持久版本相同也必须重新取正文。
- `belong[plat_belong]` 是公告ID线序权威；重复ID按线序保留，缺失ID跳过。公告再按source对plat_name前缀和服务器时间窗筛选，内容只识别 `open_login/open_inside`。登录弹出状态按账号隔离，游戏内MD5已读/红点按角色ID隔离；主动选角开始角色会话，自动重连不重复重置。
- `GameNoticeModule`同时消费登录和游戏内切片；标题根透明Image是唯一命中体，选择游戏内条目即持久化已读。417入口红点聚合公告与Welfare模型；在WelfareView外壳迁移前只允许直达公告子页。不得把10207当成功ACK、从资源CDN加载、解析失败清旧、跨角色共享红点或混接10211。

### 7.17 圣骸 14401/14402 的全量读侧与权威打造事务（2026-07-30 核对）

- GAME_START仍只发送14401的 `stage=0,type=0` 请求；其三层列表是完整全量树，每包排序后原子替换，空表loaded清旧。14402请求固定为 `stage:u8,type:u8,pos:u8`，回包先读 `code:u32` 再读同结构stage_list；两号共用结构但更新语义不同。
- 打造前置条件严格对齐服务端顺序：配置存在、角色等级足够、同部位前一阶段已打造、当前位置未打造、真实背包材料足够。equipment配置cost中指向已配置圣骸装备的条目是前阶状态条件，不得参与背包数量检查或本地扣除。
- 确认弹窗冻结选择、配置版本、ArmorModel版本与BagModel版本/数量指纹，确认时再次校验；待回包期间单飞。客户端不乐观修改树、不扣物、不猜属性；`code==1`仅按服务端回包的stage/type权威切片局部合并，失败、取消或无回包保留既有状态。
- EquipArmor页面消费权威配置和模型渲染阶段/类型/部位、真实材料、属性总览与页内红点。打造按钮容器是唯一点击面，装饰Graphic关闭raycast；专项必须通过真实Prefab `GraphicRaycaster→PointerClick`，直接调用业务方法不算点击链验收。

### 7.18 Goods 15010/15017/15018 的背包与已穿戴装备容器（2026-07-30 核对）

- `15010` 的 C2S 是 `pos:u16`，S2C 是 `pos:u16,cell_num:u16,max_cell:u16,cell_gold:u8,goods_list`；主背包 `pos=4` 与角色已穿戴装备 `pos=1` 都是各自容器的权威全量。两者必须分别原子替换，禁止把已穿戴实例只存进页面或自动穿戴私有缓存。
- `15017` 由服务端按物品真实 `location` 主动推送完整 goods 单项，字段结构与15010一致；`15018` 同样按location推送 `{goods_id:u64,goods_num:u32,type_id:u32}` 数量变化。`pos=1` 与 `pos=4` 都必须消费全量和两类增量，落模型后分别发装备/背包事件；不再把装备增量当“未接容器”丢弃。
- 已穿戴装备槽位优先按 `config_goods.equip_type` 解析；登录早到包可能先于配置加载，模型必须保留完整原始实例，并在读取时以合法 `cell=1..10` 兜底。配置完成后页面重绑一次，不能因首次映射为空永久丢失装备。
- 背包与装备格只消费模型事实：主背包按服务端cell铺格，装备区按1..10部位绑定通用 `EquipmentItem`，点击传真实 `BagGoods` 给实例tips。图标和部位配置异步加载失败时保留模型，不伪造物品、不清空旧协议数据。

### 7.19 Designation 41109 的道具激活事务（2026-08-02 核对）

- 41109不是查询。C2S严格为 `id:u32`；服务端从 `config_dsgt.goods_consume` 读取恰好一条ObjectList消耗，校验未激活与背包材料后真实扣物、激活、落库、重算属性并触发任务，S2C固定为 `errcode:u32,power:u32,currentused:u32,dsgtid:u32`。
- 唯一入口是现有可编辑 `DsgtModule.prefab` 的称号详情激活按钮。按钮只在41101权威列表已加载、目标未激活且配置存在一条合法type=0材料时显示；数量与红点读取BagModel真实持有量。点击时控制器再次执行相同门禁，并要求背包全量已加载和材料足量。
- 同一控制器只允许一个41109在途，10秒无响应后才允许玩家重试。回包无论成败都完整替换独立raw结果并解除事务单飞；失败不改41101。成功也不本地扣物、不用 `currentused/dsgtid` 乐观补丁列表，而是提示成功并精确空发一次41101；该全量回包到达前同一称号保持“刷新中”并禁止再次激活，让服务端全量成为激活状态、阶数、过期时间与当前佩戴的唯一权威。
- 激活按钮根节点运行时补透明Image作为唯一Graphic点击面，背景和文字等子Graphic关闭raycast；验收必须从真实Prefab克隆按钮走 `GraphicRaycaster→PointerClick`。41109不加入GAME_START，不从41104自动佩戴。41106升阶已在7.22完整续接；41102/41103场景佩戴和41110过期取消仍保持DEFER。

### 7.20 Achievement 40904/40907 的增量与全量隔离（2026-08-02 核对）

- 40904和40907都没有C2S read分支，只由服务端成就状态变化主动推送；不得为两号添加sender、GAME_START请求或自动补查。现有启动序列仍严格为 `40901 -> 40903 -> 40906 -> 40908` 四个空帧。
- 40904为 `updates:u16×{id:u32,status:u8,progress:u64}`。每包先完整替换独立不可变raw增量；只有40903权威列表已经加载时，才按wire顺序替换首个同ID缓存条目的status/progress并保留原category。未知ID不补项，重复ID最后一条生效，空增量只把raw切片置为空，不清40903全量。
- 40907为 `rewards:u16×{stage:u32,status:u8},cur_stage:u8,new_cur_stage:u16`。每包完整保存不可变raw增量；只有40901权威阶段快照已加载时，才按wire顺序以stage键覆盖既有奖励或追加新奖励，并更新两个阶段字段。空增量仍更新阶段字段，但不清已有奖励；早到包只存raw，不伪造已加载的40901。
- 后到40901/40903全量始终可以整体覆盖合并结果，并且不会反向清除两份最后raw增量。40902/40905是发奖并持久化的真实领取事务，40909是成就分类页面显式查询；在成就配置、页面、按钮门禁、奖励/背包与错误闭环迁移前均不公开入口，也不接事件、红点、Toast或本地奖励。

### 7.21 SentientAct 24106 门户销毁通知（2026-08-02 核对）

- 24106没有服务端请求处理语义，运行时只由众生之门进程在 `lists:keydelete` 销毁一个门户后向所在场景广播；S2C body固定为 `portal_id:u64`。老端收到后先通知场景删除表现，再严格空发一次24102重取门户全量。
- Unity当前尚无该活动的场景门户消费者，因此24106只保存一份不可变最后raw通知，并沿用既有 `RequestPortals()` 精确空发一次24102；不得添加同号sender、GAME_START请求、场景对象销毁、事件、UI、配置、红点或Toast。
- 24106不补丁式修改24102列表。即使命中重复ID、`0`或`u64 max`也完整保存raw，重查无回包时旧门户快照保持不变；后到24102仍按wire保序保重整体覆盖，并且不会清24106 raw。登录Reset与Dispose同时清两份切片。
- 启动序列保持 `24101→24107→24102`；活动态24101原有的一次24102追查也不变。24103进入门户、24104/05/08场景与组队动作、24109副本信息等仍等待完整场景/队伍/UI业务闭环。

### 7.22 Designation 41106 的称号升阶事务（2026-08-02 核对）

- 41106不是查询。C2S固定为 `id:u32`；S2C固定为 `errcode:u32,order:u8,power:u32,currentused:u32,dsgtid:u32`。服务端只允许已激活称号升阶，要求当前阶和下一阶配置都存在，并按当前阶 `config_dsgt_order.consume` 扣除真实物品、写DB、更新被动技能与属性战力。
- Unity迁入老端当前CDN法定源的4772条`config_dsgt_order`，并加入`ClientConfigSync`的server同步清单。只有41101权威实例已存在、基础配置`main_type=3`、当前阶低于`order_limit`、当前/下一阶均有配置，且当前阶consume恰好一条合法type=0物品时，详情页才显示“升阶”；材料格、持有/需求数量和足量红点读取真实BagModel，属性展示切到当前权威阶配置。
- 点击仍复用可编辑`DsgtModule.prefab`现有动作按钮，按钮根透明Image是唯一点击面，装饰Graphic不截射线。控制器在发送前再次执行全部状态/配置/背包门禁；41106与41109共用页面级互斥单飞，10秒无回包后才允许人工重试。
- 每个41106回包完整替换独立raw结果并解除单飞。失败不修改41101；成功也不本地扣物、不采用回包`order/currentused`补丁实例，而是精确空发一次41101。全量到达前同一称号保持刷新门禁，后到41101整体决定阶级、佩戴态和过期时间，并驱动当前阶属性/下一阶材料重新展示。
- 41106不加入GAME_START，不发本地属性/战力或奖励事件。41102/41103仍依赖未迁移的场景称号表现，41110仍缺产品入口与结果消费，继续保持DEFER。

### 7.23 Revelation 286xx 的负约束与权威全量（2026-08-02 核对）

- GAME_START只严格空发28606；28606原子替换形象ID、u64总战力、聚灵/套装/技能三张有序全量，空表loaded清旧。28609仅允许业务显式空查，未加载28606时读完忽略，已加载时只替换Power并保留其它字段。
- 28608理论上是套装表S2C补丁，但当前服务端`pp_revelation_equip`没有同号handler，业务全仓只有`pt_286` codec且writer零调用；它保持KILL，不得注册或覆盖28606套装。若未来服务端恢复真实推送，必须先审清与28606全量的时序再重审。
- 28600错误出口虽在服务端活跃，但R123/R137建立的原始负约束明确排除28600-05/07/08；晚于该规则接入的28600 raw已在R539撤销。28601-05/07会真实穿戴、卸下、吞噬、升级技能或改变形象，仍须等待配置、背包、属性、场景外观、UI与错误/成功闭环整体迁移。
- 286族保持pending。负约束纠偏允许raw注册数诚实下降，不得为覆盖率重新加入28600或用killlist伪造真实写事务已完成；baseline历史计数不随单轮改写。

### 7.24 跨族负约束时序审计与豁免条件（2026-08-02 核对）

- R118、R126、R125、R129分别在AGENTS原权威段明确排除11201-11205、13505-13510、21600/21602-21606与27902-27909。后续11205、13507、21600/21606、27904-27909虽然各自有双端取证或新增正向说明，但都晚于原禁令，且实现轮没有同步改写原负约束；按R517/R539确立的优先级，这十号必须撤销。
- “只读查询”“S2C-only通知”“统一错误出口”或“先落raw地基”只描述协议副作用，不等于产品授权。若AGENTS已有明确不接裁决，后续实现前必须先在同轮给出新的产品授权、双端可达性与完整消费者证据，并直接改写原负约束；仅在文件顶部另加一条正向记录会形成互相矛盾的双重权威，不构成豁免。
- R540删除十号的Proto常量、注册、sender、handler、模型切片与正向用例，只保留11200四类装扮全量、13500/13503/13504九魂读侧、21601魔法阵按需全量、27900/27901永恒圣殿时程与资格。对应家族继续pending，真实活协议不进入killlist。
- 纠偏轮允许`registered`下降、`liveGap/errorExit`上升；这代表治理口径恢复诚实，不是功能倒退。`baseline.json`冻结计数不得追报告改写，专项回归应把被撤销号改成负向注册矩阵，阻止同类增量再次混入。

### 7.25 Game/BrightSea/SnatchTreasure 晚增纠偏（2026-08-02 核对，R545补漏）

- R111的KfStage段先明确不接10205，R179/R167-R179先明确不接18903/18905-18914与18918-18920，R170先明确65208归ActivityForeshow且不接65206。`git blame/log`确认10205、18905、18918/18920、65206实现都晚于这些原禁令，且对应实现轮只新增正向记录，没有直接改写原裁决；R541先撤销10205、18918/18920、65206，R545再补撤被区间写法漏审的18905。
- GameStart保持10201/10202/13088等既有注册；10205虽然是服务端全局活错误出口，也不得注册Toast、日志或raw占位。BrightSea只保留18900/01/02/04/15/16/17，18905启动结果、18918退出失败与18920异步错误均不落本地切片。SnatchTreasure只保留65201显式入口快照，禁止公开65206退出sender或接结果，65208仍由ActivityForeshow唯一持有。
- 五号均为真实活缺口，继续pending且不得写入killlist。专项用例必须对10205、18905、18918、18920、65206做负向handler矩阵，并精确恢复其余合法handler、模型、事件和拦截器。
- R542已把13218从“时序例外待裁决”收口为语义假阳性：`14ec2c02c6`在同一提交里明确以既有13218为数据底座，并把范围写成“不接13218联动”。因此13217段的英文短句只禁止13217请求、触发或清理13218，不禁止`48fd16be92`已建立的独立S2C-only主动推送。13218继续无sender、逐包覆盖自动熔炼经验且与13217双向隔离。

### 7.26 扩展负约束表达全量交叉审计（2026-08-02 核对）

- R542审计解析Proto数值常量及别名常量，复原当时全部1261个运行时注册号，再与AGENTS中的“不接/不注册/排除/KILL/absent/exclude/do not attach”等逐句交叉；但人工候选提取没有把`18905-18914`区间完全展开，误报“无新冲突”。R545重新逐号展开区间后发现18905仍是晚增违规并完成撤销。后续审计必须先规范化区间再做交集，不能只比较显式数字端点。
- 机器交集不能直接替代句义：同一条规则常同时列出“保留的权威号”和“排除的操作号”，也可能只禁止sender、GAME_START、UI或跨切片联动。只有明确约束常量/注册/handler本身的协议号才进入时序审计；局部副作用限制不得误扩成全局禁号。
- 13218是本轮唯一需要提交历史消歧的命中。老端注册并消费主动推送，服务端在自动熔炼成功后与15024并列发送，Unity现有handler无C2S且与13217隔离；保留实现比按截断短句误删更符合原始权威记录。

### 7.27 Adventure 42704 晚增纠偏（2026-08-02 核对，R546）

- R546直接读取`baseline.next.json.registeredCmds`中的1261个真实运行时注册号，把AGENTS内`A-B`、`A/B-C`与完整端点区间统一展开后逐句裁决。新增范围交集中，`39901`只禁止C2S手动开局而明确保留S2C开始通知，`65208`由同句明确归ActivityForeshow独占，`18315`只禁止会写日计数的C2S请求而允许S2C重置推送；三者均不构成全局注册冲突，唯一剩余违规交集是42704。
- R155提交`57327cc843`在工单中明确“本轮不接42704-06商店”，并把AGENTS边界写为`Do not attach ... 42702-06`。R156提交`a73889d26a`六分钟后追加42704完整只读快照，却只在原句下一行追加正向记录，没有同步改写原负约束或记录产品授权；按负约束优先规则必须撤销。
- Unity删除42704常量、注册、显式sender、handler、`HasShopSnapshot/ShopTimes/RefreshCost/ShopGoods`模型树及正向用例；`AdventureCase`改为断言42702-42706全部无handler且`On42704`不存在。42700活动时间/图标和42701显式主状态保持不变；42704仍是服务端只读活缺口，家族继续pending且不进killlist。

### 7.28 协议硬负约束机器门禁（2026-08-02 核对，R547）

- `hard_negative_constraints.json` 只表达“现行产品裁决禁止 Unity 定义/注册”的边界，不判定协议死亡。R517与R539-R546撤销的20号仍按双端事实留在liveGap/pending，不进入killlist，也不改变baseline历史分子或分母。
- `ProtocolCoverageCase`新增F段：逐条要求`cmd/rule/evidence`完整且协议号唯一，并同时检查运行时`NetManager._handlers`、源码静态`RegisterProtocal`调用点和`Proto.cs`公开常量。任一层重新出现都失败并打印具体位置；清单缺失或空表同样失败，避免删除门禁文件绕过检查。
- 首批范围为`10205,11205,13507,18905/18918/18920,20600/20602,21600/21606,27904-27909,28500,28600,42704,65206`。13218不在清单：R542已证明它只受“禁止与13217联动”的局部约束，独立S2C注册合法。未来产品若解禁某号，必须同轮改写原AGENTS裁决、移除/调整清单项并完成真实消费者；清单不能单独替代产品授权。
- 反向验收临时把已注册10201加入清单，F段同时从运行时handler、`Proto`常量和`GameStartController.cs:91`静态调用点命中并以`EXIT 3`失败；移除探针后的`coverage_20260802_171521.md`加载正式20条约束，A～F全PASS、`EXIT 0`。这验证了三层检查均参与最终裁决。

### 7.29 killlist、baseline 与运行时/发送边界对账门禁（2026-08-02 核对，R548-R553）

- `killlist.json`描述的是当前不应存在S2C消费者的死号或半死号，不等价于“协议号在Unity完全不存在”。40218/62107等真实C2S操作可以保留发送常量，但服务端从不写的专属同号结果不得挂handler；G段因此以运行时`NetManager._handlers`为裁决面，同时检查killlist重复号。
- R548逐项审计原182条killlist与13个运行时交集。Baby 18208/09/16/17/18/19/20/24已被后续完整UI/事务轮次恢复，19501已有真实角色卡查询链，40230当前服务器守卫成功分支仍写专属结果，十号属于过期死亡裁决并从清单删除。13802唯一writer调用已注释；19007只有老端不可达查询和空handler；33225/26老端零sender且handler体注释，四号删除常量、注册和孤立模型并继续留在killlist。
- 冻结baseline仍保存四个已删除handler。A段只允许“baseline中缺失且当前killlist/硬负约束带evidence”的具体号消失，B段按相同具体号补偿对应家族计数；当前总注册仍必须不低于历史总量，任何无裁决号或未补足的家族减少继续报红，禁止改写baseline掩盖移除。
- 负向探针临时把现役10201加入killlist，G段精确报告`死号仍有运行时handler:10201`并以`EXIT 3`失败；移除探针后正式172条清单零交集，`coverage_20260802_173442.md` A～G全PASS。
- R549进一步把常量例外从“G一概不检查”改为逐条声明。`KillEntry.clientMode`缺省为`absent`，该模式既不能注册handler也不能保留Proto常量；`send_only`表示C2S真实可达但S2C死亡，必须有常量且仍不得注册。当前七个例外固定为11101秘籍执行、30008加入结社任务事件、40029调戏成员、40218退出晚宴、40505/40507神像铭文穿脱、62107退出资格赛；每号均已有生产发送方法和服务端处理链。
- `clientMode`未知值、absent残留常量、send-only缺常量都会让G失败。临时撤掉11101声明后，正式常量被判为`absent死号仍有Proto常量:11101`并`EXIT 3`；恢复后的`coverage_20260802_174537.md`显示172条、`send_only=7`、A～G全PASS。不得用send-only为没有真实C2S消费者的死号保留常量。
- R550把“已有生产发送方法”的人工事实也变成静态门禁。Scanner只遍历`Assets/Scripts/**/*.cs`，复用去行/块注释结果并识别跨行`Send*(Proto.X,...)`；Editor测试、注释、handler注册、常量定义、局部变量间接传参和普通引用均不计入`UnityStaticSendSites`。G要求send-only存在直接发送点，absent不得存在。
- 11101反向探针临时改为`int command=Proto.GM_CHEAT_EXEC; SendFmt(command,...)`：代码仍可编译、常量仍存在、发送行为也未消失，但可审计的直接引用断开，G以`send_only缺生产发送引用:11101`和`EXIT 3`拒绝；恢复后的`coverage_20260802_175259.md`显示七号各有发送引用且A～G全PASS。新增例外应保持直接常量调用形式，不得用别名绕过机检。
- R551补上数字字面量旁路：Scanner新增`UnityStaticLiteralSendSites`，识别生产源码中的五位协议号`Send*(12345,...)`，它只能作为F/G负向证据，永远不能替代send-only所需的`Proto.X`具名直发。为避免把日志/XML文案中的发送样例误报，扫描前先把普通、逐字字符串和字符字面量等长替换为空格，再去行/块注释，行号仍可精确回报。
- 10205反向探针临时加入不可达`SendFmt(10205)`，F精确报告`literalSend@Assets/Scripts/Module/Core/Gm/GmCheatController.cs:55`并以`EXIT 3`失败，G同步显示`数字直发=1`；移除后`coverage_20260802_180703.md`显示`数字直发=0`、七个send-only仍有七组具名发送引用且A～G全PASS。生产协议发送必须直接引用`Proto`常量，不得以数字或别名规避审计。
- R552把活缺口从“只有族级计数”升级为逐号清单。`FamilyStat.LiveGapCmds`与`baseline.next.json.families[].liveGapCmds`按升序保存每族真实`LiveDefined-UnityRegistered`；正式`baseline.json`的历史计数与状态不自动改写，旧基线缺字段仍可兼容读取。
- 覆盖报告新增“活缺口逐号清单”，按killlist、硬负约束（排除与killlist重叠）和未落机器清单三桶列出。`coverage_20260802_181516.md`核对85族/331号，79+20+232恰好等于331；“未落机器清单”只表示尚未进入两份机器治理表，可能已有DEFER文档，绝不构成接入授权。
- R553新增H段反向状态门禁：对正式baseline中每个非`done`家族按当前扫描重算活缺口；若已是零缺口，或全部缺口都被带非空evidence的killlist覆盖，则必须失败并要求同轮转`done`。H同时拒绝非法状态和正式baseline漏掉当前家族，和C段共同形成“未治理不得done、已治理不得继续pending”的双向约束。
- `baseline.next.json`不再机械覆盖人工策展：候选保留正式baseline的`status/statusNote`，并独立输出由当前逐号缺口和有效killlist计算的`suggestedStatus`。因此150/152仍可如实保留`legacy_unverified`，同时显示机器建议`pending`；候选仅供审阅，禁止据此改写冻结计数或跳过人工裁决。
- 反向探针把144零缺口族和400全killlist族临时改为`pending`，H精确报告`144[零活缺口]`与`400[全killlist:...]`并`EXIT 3`；逐行恢复正式baseline后的`coverage_20260802_182556.md`为A～H全PASS、`EXIT 0`，运行时指标仍为`1256/331/20/77.5%`。
- R554继续收紧正式baseline的人工编辑完整性：H要求`families[].prefix`唯一；对当前仍有活缺口的`done`家族要求非空`statusNote`，使killlist收口理由不会在后续人工整理时被静默抹掉。零缺口done族不强制补历史说明，避免为90个已自然全覆盖的旧族制造无信息文本。
- 当前正式baseline共179族且无重复；17个“仍有当前活缺口但已done”的家族全部有说明。反向探针临时复制144条目并清空400说明，H同时报告`baseline家族重复:144`与`带活缺口done族缺statusNote:400`并`EXIT 3`；恢复后的`coverage_20260802_183414.md`为A～H全PASS。
- R555补齐G段自身的killlist schema门禁：清单必须非空，每条`cmd`必须在10000～99999且`reason/evidence`非空。此前G只检查重复、运行时交集、clientMode、常量和发送引用，空证据只会在C/H计算有效kill集合时被间接忽略，pending家族中的坏条目可能不挂红且仍被正向计数。
- 当前172条均通过五位cmd、reason和evidence审计。反向探针清空10204的evidence与10208的reason后，G精确报告`cmd/reason/evidence不完整:10204,10208`并`EXIT 3`；恢复后的`coverage_20260802_184050.md`为A～H全PASS，G明确显示172条字段/evidence完整、7个send-only发送证据齐全。
- R556把hard-negative与killlist的语义分离也变成F段硬门禁：前者是“真实活但当前产品禁止接”的pending边界，后者是“死亡/半死亡”裁决，任何同号重叠都会让报告分桶和baseline收口理由产生矛盾，不能再只在Markdown展示时从硬负约束桶静默排除。
- F同时把hard-negative的cmd从旧`>0`收紧为10000～99999，并保留非空`rule/evidence`要求。当前20条硬负约束与172条killlist零交集；反向探针加入204和10204后，F精确报告`cmd/rule/evidence不完整:204;与killlist重叠:10204`并`EXIT 3`。恢复后的`coverage_20260802_184844.md`为A～H全PASS，两份正式清单均未改。
- R557再要求每条hard-negative必须属于本次扫描的`LiveDefined-UnityRegistered`。一旦某号已实现、被新证据判死或从老端活集合退出，继续留表会错误参与A/B历史移除豁免和报告分桶；F因此把“不在当前liveGap”视为陈旧约束并要求同轮重审，而不是自动迁移到killlist。
- 当前20条全部在331个liveGap中。反向探针加入字段合法、无注册且不与killlist重叠的99999后，F只以`不在当前liveGap:99999`精确失败并`EXIT 3`；恢复后的`coverage_20260802_185443.md`为A～H全PASS，F显示20条均属于当前liveGap且其余边界不变。
- R558保护A/B所依赖的冻结`registeredCmds`逐号清单。正式baseline的`totalUnityRegistered=893`与893个历史注册号一一对应；数组必须等长、五位、唯一并严格升序。否则即使当前1256仍高于历史总数，A也必须失败，避免人工删空数组后只剩宽松的总量比较，或让同家族换号绕过B的族级计数。
- B读取显式空/损坏数组时改为安全空集合，不因null异常中断报告，但A会把结构损坏挂红。反向探针把10003改为重复10000并删除10004，A精确报告`registeredCmds计数892!=历史总量893;registeredCmds重复:10000;registeredCmds非严格升序`并`EXIT 3`；恢复后的`coverage_20260802_190102.md`为A～H全PASS，正式baseline未改。
- R559进一步校验`families[].unityRegistered`必须逐族等于同一份冻结`registeredCmds`按`cmd/100`分组后的数量；179族总和当前恰好为893且逐族零偏差。否则人工下调某族历史门槛会让B误认为家族未倒退，即使A的逐号清单本身仍完整。
- B在当前注册回归比较前先报告基线内部不一致，并检查逐号清单中的任一前缀都存在正式家族条目。反向探针只把100族历史计数5下调到4，B精确报告`100:族计数4!=逐号清单5`并`EXIT 3`；恢复后的`coverage_20260802_190831.md`为A～H全PASS，B显示179族冻结计数与893条清单一致。
- R560把另一组冻结事实也纳入B：179个`families[].liveGap`必须全部非负且总和精确等于顶层`totalLiveGap`，顶层历史活缺口还必须位于`0..totalLiveDefined`。正式baseline的分项总和当前为697，与顶层697一致；这阻止人工单改某族历史缺口或顶层汇总后留下彼此矛盾的防退基线。
- 反向探针只把100族`liveGap`从0改为-1，B同时精确报告`baseline家族liveGap为负:100`和`baseline家族liveGap汇总696!=历史总量697`，总判决失败；恢复后的`coverage_20260802_191606.md`为A～H全PASS，正式baseline未改。
- R561继续校验顶层派生字段：`liveCoveragePercent`必须等于`round(100*(totalLiveDefined-totalLiveGap)/totalLiveDefined,1)`，零分母时约定为0。正式冻结总量表示`771/1468=52.5%`；只把百分比改成52.6的探针被B精确报告`baseline历史覆盖率52.6!=总量推导52.5`。
- 恢复后的`coverage_20260802_192317.md`为A～H全PASS，B详情同时显示893条冻结注册号、历史活缺口分项/总量697与覆盖率52.5%一致；正式baseline未改。
- R562把全部顶层历史总量还原成互斥分区：`2371=活覆盖771+活缺口697+非活已注册24+deadGap879`，定义内历史注册共795；再由`totalUnityRegistered=893`推出手写98。`ProtocolCoverageReport.DenominatorNote`新增数值重载，扫描报告与A段基线核验复用同一生成函数，避免两份格式逻辑漂移。
- A先校验上述分区均非负且不越界，再要求冻结`denominatorNote`逐字等于统一函数输出。`deadGap=904`探针被报为非法分区；数学仍合法的878探针则被精确报为口径文案与冻结总量不一致。恢复后的`coverage_20260802_193328.md`为A～H全PASS，正式baseline未改。
- R563追溯轮22提交`2b281f943`的“族错误出口欠账50→34”，用当前错误出口语义候选减去冻结893条`registeredCmds`，精确重建出34号历史集合。正式baseline新增只读证据`errorExitUnregisteredCmds`，不改34这一冻结计数；字段要求等长、五位、唯一、严格升序。
- E除继续守当前未注册数不超过34外，还要求当前20号全部是历史集合子集，并拒绝“按历史注册清单重建”时出现清单外候选；历史项后续被实现或语义不再识别允许减少。候选baseline同步输出当前20条逐号清单。`count=35 + 10204→204`探针同时命中计数失配、非五位号及当前/重建新增10204；恢复后的`coverage_20260802_194320.md`为A～H全PASS。
- R564新增I段“候选基线完整性”，把A～H扩为A～I。I重新从同次扫描计算`generatedAt/denominatorNote`、六项顶层计数与覆盖率，逐项比对1256条`registeredCmds`、20条当前错误出口，以及179族`unityRegistered/liveGap/liveGapCmds`；候选家族必须与当前扫描精确同构，正式策展家族不得丢失。
- 人工`status/statusNote`按正式baseline逐族字面保留，新族才用机器建议初始化；`suggestedStatus`独立按当前`liveGapCmds`是否全属于带evidence killlist计算。故障注入让候选`liveGap+1`并清空`StatusNote`，I同时命中顶层`totalLiveGap/liveCoveragePercent`和21个说明丢失家族；恢复后的`coverage_20260802_195105.md`为A～I全PASS。
- R565修复报告生成器的两个真实漂移：总量表原来读`baseline.ErrorExitUnregisteredCount`，把历史34误写成本次值，而E/日志实际为20；断言标题又硬编码A～H，已漏掉I。现在总量表明确写“当前20”，baseline对比另列`20 vs 34(delta -14)`，断言范围按实际条数动态生成。
- 新增J段“报告正文自洽”，先检查抬头、七项当前总量、错误出口当前/历史对比、动态断言范围和已有九条正文，再加入J并重建最终报告；最终文本若仍有未被J捕获的问题则异常退出。故障注入恢复“写34+标题A-H”时，J精确报缺当前20行及标题非A-I；恢复后的`coverage_20260802_195841.md`为A～J全PASS。
- R566新增K段“报告明细表自洽”。K从扫描结果逐行重建家族表的注册/活缺口/死缺口/路由/策展状态，要求179行按活缺口降序且汇总为`1256/331/883`；再重建85个有活缺口家族的逐号表，要求killlist、硬负约束、未落机器清单三类互斥分组合计331号。
- 故障注入让生成器同时跳过189族，K精确报告家族表179→178、注册1256→1249、活缺口331→318，以及逐号表85→84/331→318，并定位字段/分组缺失前缀189。恢复后的`coverage_20260802_200552.md`为A～K全PASS。
- R567新增L段“legacy报告自洽”，按正式baseline顺序重建每个`legacy_unverified`家族尚未被带evidence killlist治理的活缺口；当前精确为150族`15011,15028`和152族`15218,15221,15222`，共2族/5号。报告生成器与K也收紧为只有证据非空的killlist、以及协议号/规则/evidence均合法的硬负约束才参与分组，失败报告不再替坏清单掩盖缺口。
- 组合故障注入清空15023 evidence并让报告漏掉150族：C/G按清单完整性失败；K继续PASS且把15023重新列入未落组；L精确报`legacy行数1!=2`并定位150。恢复后的`coverage_20260802_201419.md`为A～L全PASS，正式baseline与两份治理清单均未改。
- R568在报告新增“活缺口治理分类汇总”，把同次扫描的331个活缺口显式分成有效killlist 79、有效硬负约束20和未落机器清单232。新增M段独立重建四行，K守逐族逐号明细、M守跨族分类总量，避免读者手工汇总85行或总数正确但分类漂移。
- 故障注入只把汇总改成`80+20+231=331`，保持合计不变；M仍精确报告期望/实际四行差异并令总判决失败。恢复后的`coverage_20260802_202150.md`为A～M全PASS，正式baseline与两份治理清单均未改。
- R569三路复核Goods遗留15011/15028。15011旧端虽有真实查询入口，但服务器与现役19502从同一装备权威源输出相同九字段元组；现役19501入口还统一同服/跨服并携带法阵/仙灵，Unity全部资料卡入口均已收口至`LookOverFlow→19501`，因此15011进入有证据killlist。15028服务器会真实删料、产物并回奖励，而Unity合成页仍只是缺配置/模型/材料选择/资产结果闭环的骨架，故新增hard-negative而不接孤立协议。
- 临时加入15011/15028常量时，G分别报告`15011[ProtoConst]`死号复活、F报告`15028[ProtoConst]`硬负约束违规，总判决为FAIL；撤回后`LookOverCase`的modules/wire/filter/cache/clear/compare全PASS，`coverage_20260802_203222.md`为A～M全PASS。注册/活缺口保持`1256/331`，治理分类变为`80+21+230=331`，legacy区收敛为150族15028与152族三号，共2族/4号；baseline历史机器计数未改，仅补150族人工状态说明。
- R571修复legacy报告晚于K/M形成的语义漂移：hard-negative虽不表示业务完成，却已经是带规则和证据的机器治理，故报告生成器与L都必须把五位cmd、非空`rule/evidence`的有效项从“未申报”中排除。L从原始清单独立重建集合，字段损坏仍会回落为未治理；C/H的done收口和候选`suggestedStatus`继续只认有效killlist，禁止用hard-negative把真实活事务伪装成完工。
- 负向探针临时恢复旧生成器后，报告重新出现150/152两行和15028、15218/21/22四号，L精确报`legacy行数2!=0`、前缀不一致及缺`(无)`标记。恢复后的`coverage_20260802_205055.md`为A～M全PASS，L为`0族/0号`，治理分类仍为`80+24+227=331`，注册、liveGap、baseline及两份治理清单均未改。
- R572进一步收口人工状态三态：`legacy_unverified`只允许当前仍有未落入有效killlist/hard-negative的未分类活缺口；若非空活缺口已全分类且至少一项仍为hard-negative，则业务尚未完成但证据已核清，必须转`pending`。零缺口或全killlist仍必须转`done`，hard-negative始终不能充当完工证据。
- 正式baseline只把150/152从legacy改为pending，冻结的历史`unityRegistered/liveGap`与既有`statusNote`均不变。H现独立重建有效hard-negative集合；回退两族时精准列出150的三号和152的四号并FAIL，L仍PASS。恢复后的`coverage_20260802_205858.md`为A～M全PASS，家族表150/152均为pending，L=`0族/0号`，治理分类与raw指标不变。

### 7.30 AutoBrush 13310 阶段奖励事务（2026-08-02 核对）

- 13310不是读查询。C2S固定为`gate:u64`，S2C固定为`code:u32,u16×{style:u8,type_id:u32,count:u32}`；服务端要求请求gate等于角色下一阶段奖励gate且当前关卡已达标，成功后向客户端回实际奖励、发奖并写入新的阶段gate。
- Unity只允许用户点击现有`AutoBrushModule.prefab`的`_box_show/_box_click`触发；发送前必须已有`13309 code==1`、gate非0且不超`long.MaxValue`、当前关卡达到gate。容器根由`UIUtil`补透明Image并作为点击面，业务代码不重建或改写Prefab视觉。
- 请求发出后10秒内单飞；每份13310回包都完整替换独立不可变raw结果。失败解除单飞、保留13309且不回查；成功展示回包中的实际奖励，只精确空发一次13309，并在该权威快照到达前继续阻止领取。
- 客户端不得根据13310乐观推进gate，不得本地扣除或增加物品，也不得把回包奖励列表解释为二次发奖。GAME_START仍严格为`13300→13301→13309→13323→13324`，13310不自动发送；13322教程节点写事务继续DEFER。

### 7.31 Equip 15218/15221/15222 资产事务边界（2026-08-02 核对）

- 15218 C2S为`pos:u8`、S2C仅`code:u32`，但语义不是孤立ACK：服务器先校验穿戴装备、阶星限制和`data_god_equip`等级成本，成功真实扣物、事务写神装等级、重算装备/角色属性并通知物品变化；事务失败分支还会系统邮件返还成本。Unity虽已有一次转换的`GodequipbuildModule.prefab`，却没有业务View，且只同步了另一用途的`config_god_equip`，缺旧页面所需`config_god_equip_level/cfg/kv/god_pos_limit`等完整权威配置与材料校验链。
- 15221 C2S为`make_type:u8,equip_pos:u8`，成功回位置、类型、阶数和套装表。服务端会扣货币及具体背包实例、写套装DB、重算属性、通知背包并推15000；失败走15200统一错误。不得只根据成功包局部修改15220，必须由完整套装模型、资产刷新和错误链共同消费。
- 15222同形请求，成功回还原后的套装状态与实际返还列表；服务端会降级/写DB/重算属性，并通过背包或邮件真实返物。它还会在换装导致原套装不兼容时由服务器内部主动执行并下发，故“只禁sender但接raw结果”仍不完整：若没有共鸣模型、背包/邮件和主动还原消费者，就会出现资产已变而页面/属性权威未闭合。
- R570将三号全部纳入hard-negative。恢复条件是配置、可编辑业务页、材料/货币/背包指纹冻结、风险确认、同页单飞、15200错误、15000/15217/15220及背包/邮件/属性刷新、15222主动下发与奖励表现成组闭环；禁止孤立常量、sender、raw handler、Toast、乐观扣物或局部补丁。临时加入三号Proto常量时，F精确列出三项`ProtoConst`并令总判决FAIL；撤回后`EquipReadCase pass=True/restored=True/EXIT 0`，`coverage_20260802_204413.md`为A～M全PASS，治理分类为`80+24+227=331`。

### 7.32 Reincarnation/Demon/Mask/Draconic 单号资产事务边界（2026-08-02 核对，R573）

- 16401请求`cell_id:u32`，回`code:u32,cell_id:u32`。服务端先按命格配置校验前置条件与消耗，优先扣物，物品不足时再尝试扣角色经验；成功持久化命格、重算并通知属性、推进觉醒任务。16400只是一份激活ID全量，不足以承载配置、材料/经验冻结、确认、任务与属性刷新，故16401不得接孤立ACK或乐观点亮。
- 50902请求`rotary_id:u8,count:u8`，仅接受类型1/2与次数1/10；回包含`code/type/count/bless_value/ObjectList`。服务端先生成随机池结果，再真实扣费、写祝福值和累计次数，最后发奖或邮件。50901祝福值标量不会替代转盘配置、钱包、自动购买、确认/单飞与奖励展示，故不得接裸sender或把回包列表本地再发一次。
- 51102是严格空请求，回`mask_id:u8,end_time:u32`且当前成功固定0/0。请求本身会取消计时器、写mask DB、清角色figure、更新并广播场景属性、写角色展示和派发装备事件；没有错误码。51101状态地基并不具备确认UI或场景外观消费者，故不能只接0/0结果或本地清字段。
- 62202请求`pos:u8,stren_type:u8`，成功回`pos:u8,stren_lv:u16`，失败走62200统一错误。服务端消耗龙语币、事务写强化等级、通知物品并重算属性；自动强化可连续执行并在材料不足时以已完成部分收口。62201/07/08/09读侧无法代替成本权威、部分成功语义、同页单飞、背包/货币/属性刷新和强化页面。
- 四号统一进入hard-negative，仍完整保留在liveGap/pending。恢复必须逐号补齐可编辑业务页、权威配置、消耗/资产指纹、确认、单飞、错误及下游状态消费者；禁止孤立常量、sender、handler、raw ACK、乐观扣费/点亮/强化或本地发奖。四常量故障注入被F逐号拒绝；恢复后的`coverage_20260802_210721.md`为A～M全PASS，治理分类`80+28+223=331`，raw指标不变。

### 7.33 KfSingleRank/Recharge/CycleRank/HotPoint/Pray 奖励购买事务边界（2026-08-02 核对，R574）

- 50704 C2S严格空包，S2C为`error_code:u32,rw_state:u8`。服务端按跨服单人排行起始层、开放状态和既有`rw_state`校验，每日奖励通过背包或邮件发放，随后写领取态与DB。50701-03/05的榜单和结算raw不能替代领取入口、资格配置、单飞、错误与奖励刷新。
- 15804 C2S为`product_id:u32`，S2C仅`errcode:u32`；旧端龙珠、礼包、限购、光环、祈愿、周卡等多个真实页面都会触发。服务端只接受直购币商品，构造真实成本并经`pay_by_goods2`扣费发放；不得把单一errcode解释为客户端可乐观完成的支付回执。
- 22704请求`type:u16,subtype:u16,reward_id:u16`，回显三键并带`error_code:u32`。服务器校验阶段奖励状态，持久化已领取标记并真实发奖。现有22700-03/05/06只读切片不能承担奖励档位、重复领取防护、背包或奖励表现。
- 33304请求`base_type:u16,sub_type:u16,grade:u16`，同号只返回成功三键；活动关闭、档位不符或背包容量不足等失败统一由33306表达。成功链在嗨点服务进程中写领取状态并真实发奖，因此不能只接成功handler或局部补丁33302/33303。
- 41502请求`type:u8`，回`type:u8,crit:u8,reward:u64`。服务端按免费次数或货币成本消费，更新日计数和祈愿DB，随机暴击发资源，并推进活动、成就与嗨点事件；41501剩余次数快照本身不足以覆盖配置、确认、资产冻结与全部结果消费者。
- 五号统一进入hard-negative并继续计入liveGap/pending。只有各自可编辑业务页、权威配置/资格、成本或次数指纹、确认与同页单飞、错误分流、奖励/背包/钱包/状态权威刷新及重复领取防护成组闭环时才逐号重审；禁止孤立常量、sender、raw ACK、乐观领态/扣费或本地发奖。五常量故障注入被F逐号拒绝；恢复后的`coverage_20260802_211450.md`为A～M全PASS，治理分类`80+33+218=331`，raw指标不变。

### 7.34 Auction/WelfareCard/PushGift/Compete/LimitLevelShop 资产交易边界（2026-08-02 核对，R575）

- 15403请求`auction_type:u32,goods_id:u64,price_type:u8,bgold:u32,gold:u32`，回`res:u32,price_type:u8`。服务器先校验竞价类型与货币，再同步调用本服或跨服拍卖进程；只有拍卖状态接受后才真实扣除绑定/非绑货币。完整结果还依赖15402广播、超价退款/邮件、拍卖关闭与钱包权威更新，不能只接成功Toast。当前老端源码也只剩结果注册和通用请求转发，实际竞价页不在仓内，Unity读侧更不构成恢复授权。
- 15902请求`product_id:u32`，回`errcode:u32`。服务器依次校验福利卡配置、已购买且仍有天数、当日未领和背包容量；成功扣剩余天数、记录领取时间、写DB，并按双倍周配置经背包或邮件发奖。老端福利卡商品按钮真实触发且成功后刷新卡片；Unity的15901有序列表不含奖励、错误或资产结果链。
- 19103请求`gift_id:u16,sub_id:u16,grade_id:u16`，回`code:u32`、同三键与`buy_cnt:u8`。服务器校验礼包激活、期限、档位、触发条件和购买上限，真实扣配置成本、发奖、写购买次数DB并替换活动内存；老端确认框、详情/提示页和奖励表现均为真实消费者，19101/19102只读列表/详情不能替代。
- 33804请求`type:u16,subtype:u16,reward_id:u16`，回同三键及`error_code:u32`。服务器校验活动开启、未领取和当日积分阈值，按世界等级计算奖励、真实发奖，将reward_id写入`score_reward` DB和内存。老端积分档位图片点击真实触发；33801/33802 raw缺少完整页面、奖励配置、资产刷新和重复领取防护。
- 61201请求`type:u16,subtype:u16,grade:u16`，回同三键及`code:u32`。普通路径会带自动购买校验扣成本、发奖、写档位`HAS_BUY`与DB；充值到账路径也会在无本号请求时主动推送成功并发奖/刷新61200。61200当前只迁移图标子集、61203仅是显式配置raw，缺完整档位/旧状态、充值分流、主动包去重、钱包和奖励闭环，故不能只禁sender后接handler。
- 五号统一进入hard-negative并继续计入liveGap/pending。只有对应可编辑页面、权威配置/资格与完整业务状态、成本/资产指纹、确认与单飞、错误、奖励/背包/钱包/拍卖/活动权威刷新、重复操作防护及61201主动推送去重成组闭环时才逐号重审；禁止孤立常量、sender、raw ACK、局部领态/次数补丁、乐观扣款或本地发奖。五常量故障注入被F逐号拒绝；恢复后的`coverage_20260802_212333.md`为A～M全PASS，治理分类`80+38+213=331`，raw指标不变。

### 7.35 VIP 45001/45002/45003/45007/45008 写事务边界（2026-08-02 核对，R576）

- 45001请求`vip_lv:u16`，回`res:u32`。服务器校验等级与未领取后，当前实现先把新`fetched_list`写DB，再加载等级奖励配置、尝试扣`reward_cost`并发奖励；成本失败只回错误而领取DB已先写，存在明确非原子顺序。未来消费者必须以服务器后续45000为权威、禁止盲重试或乐观标记，并同时覆盖成本、普通奖励、称号与错误展示。
- 45002请求`vip_lv:u16`，回`res:u32`。服务端按当前VIP和周领取态校验，写`week_list` DB、发`week_rewards`并更新内存。老端周礼包按钮真实触发，成功后重查45000并从`config_vip_config`展示奖励；Unity当前没有该页面、配置、资产刷新或重复领取防护。
- 45003请求`card_type:u8`，回`res:u32`。服务器校验卡型、配置、期限和勾玉，按升级差价真实扣费，启用卡片并计算保送VIP等级/经验；事务写卡、VIP信息与周领取态，成功后处理首次权益、属性更新和购买事件。45004卡片表与45005/06通知raw都不能替代支付确认、钱包、权益、等级经验和错误链。
- 45007请求`card_type:u8`，回`res:u32`。服务器基于`free_card`资格启用卡片，计算VIP等级/经验和周状态，事务写卡/VIP/免费领态并派发`EVENT_FREE_VIP`。老端卡片按钮有真实入口，另有停用/残留的定时自动领链；恢复时必须只允许权威入口和完整条件判断，禁止复刻盲自动领取。
- 45008请求及回包均为`hide:u8`，服务器只接受0/1。值变化时写`vip_hide` DB与角色figure，更新场景代理、广播12010属性17并写角色展示；相同值也会回显。它是场景外观写事务而非普通设置raw，必须与设置页、主角/他人展示和切场景一致性共同迁移。
- 五号统一进入hard-negative并继续计入liveGap/pending。只有完整可编辑VIP/特权卡/设置页、权威配置与资格、钱包/成本/背包指纹、确认与单飞、错误、奖励/称号/卡片/等级经验/周状态/场景外观权威刷新、重复领取防护及45001异常顺序兼容成组闭环时才逐号重审；禁止孤立常量、sender、raw ACK、成功Toast、乐观扣费/领态/隐藏或本地发奖。五常量故障注入被F逐号拒绝；恢复后的`coverage_20260802_213004.md`为A～M全PASS，治理分类`80+43+208=331`，raw指标不变。

### 7.36 Game/OnHook/AutoBrush/SevenDay 编排与状态事务边界（2026-08-02 核对，R577）

- 10211 C2S为严格空包，S2C为`uid:u32`，但它不是普通查询/快照。服务端请求入口调用`lib_popup:login`，按`data_popup`的首登、任意登录或条件规则筛选功能，并为需要重复触发的项安装周期定时器；定时回调与Temple完成路径也会主动写10211。老端将uid路由到429功能板。恢复必须同时具备配置、登录与角色生命周期、定时器取消/重排、Temple触发、功能路由及真实弹窗消费者，禁止只接空sender、最后uid raw或无消费者handler。
- 13213请求`count:u32`，回`code:u32,count:u32,exp:u64`。老端挂机页的一次/全部赎回按钮会按剩余次数和离线物品判断，必要时弹出绑定/非绑勾玉自动购买确认后发送。服务端校验次数、消耗或自动购买离线物品、更新`had_back_time`并写DB，再增加角色经验。它必须与赎回页面、权威次数/物品、成本确认、单飞、错误、资产和经验刷新一起迁移。
- 13322请求与回包均为`node:u8`。服务端把节点写入`enchantment_guard.node_id`并通过阶段奖励门槛存储链持久化后回显；老端教程步骤6发送后立即本地`SetStep(6)`，结果handler为空。恢复时必须改成权威确认或有失败回滚的完整教程状态机，禁止复制乐观推进或接孤立echo。
- 17501和17503都请求`day_id:u8`，回`errcode:u32,day_id:u8,reward_list:u16×{good_type:u8,good_id:u32,good_num:u32,good_auto_id:u64}`，分别处理普通与合服七日奖励。服务端校验配置与该日领取态，经背包或邮件真实发奖，写DB为已领取后返回展示列表；老端七日按钮、三份页面、图标/红点和奖励展示均为真实消费者。回包列表只是展示结果，不能作为第二次本地发奖。
- 五号统一进入hard-negative并继续计入liveGap/pending。只有10211完整编排链，以及其余协议对应可编辑页面、权威配置/资格/状态、消耗或次数冻结、确认与单飞、错误、奖励/经验/教程/图标红点/背包邮件权威刷新及重复操作防护成组闭环时才逐号重审；禁止孤立常量、sender、raw uid/ACK/奖励、成功Toast、乐观节点/领态、自动购买或本地发奖。五常量故障注入被F逐号拒绝；恢复后的`coverage_20260802_213736.md`为A～M全PASS，治理分类`80+48+203=331`，raw指标不变。

### 7.37 Designation 41102/41103 佩戴与场景事务边界（2026-08-02 核对，R578）

- 当前覆盖报告对411族给出`registered=7/liveGap=2/deadGap=1`，两个活缺口精确为41102/41103。41106道具升阶和41109道具激活已在R523/R526由真实Dsgt页、权威41101、配置/背包二次校验、互斥单飞与成功后唯一重查完整实现；41110不在活口径。本轮不得按旧续接提示重判这三号。
- 41102请求`id:u32`，回`code:u32,id:u32`。服务器先校验目标不是当前佩戴、目标存在且未过期；成功时将旧称号状态写为未使用、目标称号写为使用，更新角色figure和场景代理，回复自身并向周围广播41105。老端真实详情按钮在目标不等于当前时发送，并在成功回包后局部更新当前佩戴ID。
- 41103请求`id:u32`，只回`code:u32`。服务器要求id就是当前佩戴且仍存在，成功后写该称号未使用、把figure称号清0、更新场景代理并向周围广播41105。它与41102共享同一详情按钮及短节流，不能作为普通布尔设置单独恢复。
- Unity当前`DesignationFlow.RenderDetails`明确把`dsgt_adorn_button`隐藏；41105只保存隔离raw通知，`RoleVo`/场景角色没有称号更新消费，`NameBoard`注释和实现都明确仅落血条。即使局部修改41101的`CurrentUsedId`，主角和其他玩家也没有等价的称号图片/特效、遮蔽、蒙面与特殊副本表现，因此不构成闭环。
- 两号统一进入hard-negative并继续计入liveGap/pending。只有真实Prefab佩戴按钮、41101权威状态与配置/时效二次校验、互斥单飞、错误、成功后唯一41101刷新、主角/其他玩家RoleVo与NameBoard称号图文/特效、41105消费、切场景及遮蔽规则成组闭环时才恢复；禁止孤立常量、sender、raw结果、局部`CurrentUsedId`补丁、乐观佩戴/卸下或无场景表现的设置入口。双常量故障注入被F逐号拒绝；恢复后的`coverage_20260802_214627.md`为A～M全PASS，治理分类`80+50+201=331`，raw指标不变。

### 7.38 MondaysAward 17901/17903/17906 状态与资产事务边界（2026-08-02 核对，R579）

- 17901收发同形嵌套结构`pool:u16×{id:u16,rids:u16×u16}`。老端换池页复制当前选择、校验每类填满后发送整表；服务端要求类型不重复、每类数量等于配置且每个奖励属于可选列表，成功即替换角色内存`now_pool`，并在后续抽奖时与次数/任务一起写DB。它不是17908的只读别名，也不能用本地字典直接覆盖权威池。
- 17903为严格空请求，回`type:u8,pool_id:u16,draw_times:u16`。服务端先校验开放时段，再按当前池和累计次数权重随机；成功真实扣`draw_cost`、发放奖励、写个人记录与角色状态DB、推17902个人记录、同步跨服记录/传闻并推进成长福利。失败可能来自时段、成本、配置或空池并统一走17900，孤立成功包无法恢复资产或错误语义。
- 17906请求`task_id:u16`，回`code:u32`。服务端按任务配置及17904状态要求`HAS_ACHIEVE`，成功真实发奖励、改为`HAS_RECIEVE`、主动推完整17904、异步写角色周一状态DB，再回code=1；已领、未完成或缺配置都走17900。老端任务按钮、跳转、广告特殊任务、奖励图标与红点均是同一页面消费者。
- 三号统一进入hard-negative并继续计入liveGap/pending；现有17900/02/04/05/07/08六份隔离raw仅提供错误、记录、任务、时段/次数与奖池地基，不授权写操作。只有完整可编辑奖池/抽奖/任务页、权威四份配置与活动时段、17908池和17907次数二次校验、抽奖券/背包指纹、确认或节流与互斥单飞、17900错误、随机结果动画/奖励/背包/记录/次数/任务权威刷新及重复领取防护成组闭环时才逐号恢复；禁止孤立常量、sender、raw成功结果、乐观换池/扣券/累计次数/领态或本地发奖。三常量故障注入被F逐号拒绝；恢复后的`coverage_20260802_215315.md`为A～M全PASS，治理分类`80+53+198=331`，raw指标不变。

### 7.39 Medal 13402/13403/13404/13406/13407 写事务边界（2026-08-02 核对，R580）

- 13402为严格空请求，回`id:u32,honour:u64`。服务端按下一阶勋章配置校验并扣成本，写勋章等级DB，改figure.medal_id、角色展示与场景代理/广播，重算属性并依次推进任务、成就、推送礼包、Temple觉醒、活动和至尊VIP。旧端结果包还会按勋章阶段弹外观变化；只保存id/honour会丢失资产和下游状态。
- 13403请求`id:u32`，回`code:u32,id:u32,level:u16,power:u32,is_equip:u8`。服务端扣配置物品激活或升星、写头衔DB并重算属性；首次激活的`NewStar==0`分支会把其他已佩戴头衔改为未佩戴、自动佩戴新头衔，更新role/figure/scene并广播属性12。恢复时必须显式兼容这个自动佩戴副作用，不能把结果当普通升级raw。
- 13404请求`id:u32`、回`id:u32,code:u32`，会把旧头衔写未佩戴、目标写佩戴，改role/figure/scene并广播；13406严格空请求、回`code:u32`，会把当前头衔写未佩戴、清figure.title_id并同步场景。老端两号成功后都重查13405；Unity当前NameBoard只实现血条，缺主角/他人头衔消费者。
- 13407请求`cost_list:u16×{goods_auto_id:u64,num:u32}`，回`lv:u32,exp:u32,power:u32`。服务端根据具体背包实例生成删除/减量列表，更新物品字典并通知客户端，写强化等级经验DB、重算属性、同步场景战斗属性并记录逐实例消耗日志。类型总量校验不能替代实例存在、数量和并发变化冻结。
- 五号统一进入hard-negative并继续计入liveGap/pending；现有13400错误、13401勋章、13405头衔三份raw只读地基不授权操作。只有完整可编辑勋章/头衔/强化页、权威配置/等级/状态、背包实例与资产指纹、材料选择、确认与互斥单飞、13400错误、成功后13401/13405唯一权威重查、奖励/背包/属性/任务/活动/主角与他人场景头衔刷新及首次激活自动佩戴兼容成组闭环时才逐号恢复；禁止孤立常量、sender、raw结果、成功Toast、乐观晋升/佩戴/强化、本地删物或属性推演。五常量故障注入被F逐号拒绝；恢复后的`coverage_20260802_220001.md`为A～M全PASS，治理分类`80+58+193=331`，raw指标不变。
