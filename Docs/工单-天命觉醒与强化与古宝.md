# 工单:天命觉醒开启 + 全身强化 + 古宝激活 最小闭环

日期:2026-07-02(第16轮侦察代理产出,主控审定)
目标:解主线卡点 #64(100590 觉醒之路,ctype81)、#80(100720 强化总等级8,ctype31)、#92(100811 激活幽瞳,ctype89)。

## A:天命觉醒开启(100590,ctype81 Open_function)

- ⚠「功能开启」≠ FunctionOpenController(pt_138)!它专指**天命觉醒(TempleAwaken,pt_429)**的开场任务;
  服务端无轮询,唯一完成路径=客户端发 42900 → lib_temple_awaken:finish_initial_task → lib_task_api:open_temple_awaken。
- 协议:**42900** 完成初始任务(C2S 无参;回包 `error_code:i`);**42909** 前置完成态推送(`is_finish:c`,建议接);
  42901+(神殿主界面/章节/奖励)留后。
- 前置:KV(2)={100580,100590}(前置任务 100580 完成才可发);KV(6)=[{lv,48}] 等级门槛(服务端二次校验)。
- 配表:config_temple_awaken_kv.json(具名键)。
- 老端锚点:TempleAwakenEnterView.ts:220-223(点击发 42900)、OpenFun.ts:777-789(case 429)、TaskModel.ts:1861-1870(case 81:OpenFunHandler(429))。
- Unity 最小面:TempleAwakenModel(is_finish)+Controller(42900/42909)+TEMP 壳(「觉醒之路」说明+[开启]按钮发 42900;
  error_code==1 → toast+关壳,任务由 30001 推送自动刷新)。DoTask ctype81 → 开壳。

## B:全身强化(100720,ctype31 StrenSum)

- 判定:全身已穿戴装备强化等级总和 ≥8(check_equip_sum);服务端登录轮询+强化事件驱动,客户端只需真实执行强化。
- 协议(pt_152):**15204** 查询(发 "c" equip_type;回包 `res:i, equip_type:c, stren:h`);
  **15205** 强化(发 "cc" equip_type,type[1单次/2一键];回包 `res:i, res1:c, type:c, stren_info[u16×{equip_type:c, stren:h}]`)。
- 老端锚点:EquipStrenView.ts:199(单件发 "cc" equip_type,1)/:219(一键发 "cc" 0,2);EquipController.ts:312-339(on15204/on15205)。
- 配表:config_equip_stren_lv(列序 part/stren/object_list/attr_list,数字键,主键 "part@stren")、
  config_equip_stren_lv_key、config_equip_strengthen_max(主键 "stage@color@pos")。
- ★Unity 已有 `Assets/Scripts/Module/Core/Equip/Views/EquipStrenView.cs` 壳(btnStrOne/btnStrAll/btnStopAll 打日志);
  本工单=新建 EquipModel(stren_info 落库)+EquipController(15204/15205)+把壳按钮接上真实调用(这是**例外:允许改 EquipStrenView.cs**,只把日志占位换成 Controller 调用,不动布局)。
- 消耗铜币走通用货币;强化后任务由服务端 equip_sum 事件推进。
- DoTask ctype31/8 → 开锻造(EquipStrenView 已有入口?查 EquipFlow/Bootstrap 打开方式,接上即可;不行则 TEMP 壳列当前各槽强化等级+一键强化按钮)。

## C:古宝激活(100811,ctype89 ActiveSoap)

- 「妖物」=结界守护(enchantment_guard)的 soap 古宝碎片系统;任务=激活 soap 10001「幽瞳」的全部 2 片碎片
  (碎片消耗物品 1105010011/1105010012,刷图掉落;服务端集齐后 lib_task_api:active_soap 推进任务)。
- 协议(pt_133):**13320** 全量状态(请求无参;回包 `combat:l, soap_list[u16×{soap_id:h, debris_list[u16×{debris_id:h}]}]`);
  **13321** 激活碎片(发 "hh" soap_id,debris_id;回包 `errcode:i, soap_id:h, debris_list[u16×{debris_id:h}], combat:l`)。
- 老端锚点:MonsterMainView.ts:173-174(发包)、MonsterController.ts(13320/13321)、TaskModel.ts:1923-1936(case 89:开 AutoBrushBaseView——引导弱化中,壳直接开古宝面板即可)。
- 配表:config_enchantment_guard_soap(soap_id/soap_name/condition)、config_enchantment_guard_soap_debris
  (主键 "soap@debris":debris_name/cost/attr;⚠这两张 Unity 的 GameRes 可能已有 config_enchantment_guard_boss/stage_reward,别混)。
- Unity 最小面:GuBaoModel(soap→已激活 debris 集合)+GuBaoController(13320/13321)+TEMP 壳(幽瞳 2 碎片格:
  名称+消耗物品名+[激活]按钮;已激活标记)。DoTask ctype89 → 开壳。

## 共享接线(主控):Proto 常量、GlobalEvent、ControllerHub、DoTask 分支、RenderAll 挂用例。
## 红线:同前(不造假数据/错误显码/字段序逐字段/TEMP 壳从简/csproj 补行/dotnet 0 错/不跑 Unity)。
