# 工单:侍魂(剑魄同修)培养真页 + 任务引导三环(2026-07-07)

解主线卡点 **#20(100190 剑魄同修培养到1阶2星,ctype25)**,同套页面覆盖 **#34(100330 坐骑,ctype23)**、
**#57(100521 等级线,ctype90——等级线子页未移植,页可开任务暂不可页内完成)**。
用户要求:页面用**运行时快照 UiCreator** 直接复制老端(不再烤制);引导把"点哪里"指清楚;特效尽力。

## 快照采集(可复跑)

- `Tools/Conversion/petview_capture.cjs`:123123 号直登→清弹窗(舞台快照 gx/gy 找 close,修复了嵌套树坐标越界)→
  `GlobalEventSystem.Fire('SWITCH_MOUNT_PET_VIEW', idx)` 开坐骑/同修页签→存舞台快照+截图。
- 产物:`output/manual_round/oldclient_pet_20_horse_stage.json` / `oldclient_pet_21_partner_stage.json`(+png)。
- 结构剖析:OutWardBaseView(720×992) 挂在老端 BaseWindowSkin `_gp_item_con`,四页签[御风云骑/剑魄同修/神巫/天妖灵魄]。

## 页面(UiCreator 路线,对标 SettingCreator 范式)

- `Assets/Editor/UiCreator/Pet/PetCreator.cs`:快照 1:1 重建 `Assets/Prefabs/UI/Pet/PetModule.prefab`
  (原地覆盖,GUID/地址不变)。105 个 Bind 字段全量赋值;隐藏件(donw_group_2/btn_group_2/critGroup/
  select_btn/preview_image/skillImg)走 .scene 设计值兜底;战力/装备位/魔晶/技能位/培养材料烤既有模板
  prefab 起步实例(FightingShowSmallItem/PetEquipOutItem/PetRoundItem/BaseAwardItem);
  lv_button 下挂 main_ui_guide_select/finger 两个 UIEffectSlot(键值对齐 HudTaskTeam)。
  入口:神霄/重构UI 生成器 → Pet → 生成。
- 老端命名陷阱已保留:shadow_group 子节点叫 star*(灰底星)、star_group 子节点叫 shadow*(亮星)。

## 数据/交互(OutWardBaseView 重写)

- `Module/Core/Pet/Views/OutWardBaseView.cs`:SetType(1=坐骑/2=同修) 切对象并补拉 16002/16028;
  渲染阶名(config_mount_stage name,按职业回退 career1)/N阶/星级亮灰/战力/祝福值环(fillAmount)/自动购买态;
  lv_button=一键提升 → OutWardController.StarUp(16023)(3/4/5 走 16005);EVT_OUTWARD_UPDATE 刷新。
  幻化/背包/属性/等级线切换等未移植子窗 → toast+日志降级。
- `OutWardConfigs` 增读 config_mount_stage(GetStageName/GetMaxStar)。
- `PetFlow` 重写:BaseWindowSkin 窗框 + ConfigureShared 四页签(神巫/天妖灵魄 disabled),
  `Open(tabIndex)` 直达页签;对标老端 MountPetView 容器。

## 引导三环

1. **任务项箭头**(已有):MainUITaskItem + configtaskarrow in_main_ui。
2. **主界面图标手指**(新):`MainUIDownView.RefreshIconGuide`——主线培养任务未完成且窗没开时,
   手指指宠物图标(映射 `MainUIModel.GetGuideIconRes`:23/25/90→pet,对标老端 SELECT_STORY_TARGET PetIcon;
   in_main_ui 的 not_show_in_task_item=true 表示箭头归图标)。窗开(EVT_BASE_WINDOW_OPENED)让位页内引导。
3. **页内步骤**(新):`TaskGuideConfigs.GetInViewGuideCfg`(in_view/sub_dun_type[].in_view,step 从 1 起)+
   `OutWardBaseView.RefreshGuide`——未完成→指 lv_button(step2 文案"消耗资源提升剑魄同修等级升阶");
   达成→指窗框关闭钮(step3"点击关闭界面")。对标老端 story_obj_list=[null,lv_button] 与 _img_return。

## 路由

- `TaskModel.DoTask`:23/25/90 → `PetFlow.Open(页签)`(真页);24/92/41 仍走 OutWardShellView
  (翼影/符相/神兵页=RoleView 页签,下一批同法接入)。存量用例期望已同步(100330→"PetFlow")。

## 验证

- 离线编译:`dotnet build yu_client_unity.slnx` 0 错 ✅。
- 快照名预检:PetCreator 58 个 SnapRect 节点名 100% 命中快照子树 ✅。
- CliVerify:`Assets/Editor/CliVerify/Cases/PetTrainCase.cs`(重建 prefab→16002 合成包→渲染断言
  阶名/1阶/星级/祝福/按钮/引导槽→16023 升星→断言 2 星刷新;截图 Temp/pettrain_partner_*.png)。
  入口 `CliVerify.PetTrain`,已挂 RenderAll。**需编辑器空闲**:
  `Unity.exe -batchmode -projectPath . -executeMethod Shenxiao.EditorTools.CliVerify.PetTrain -logFile Temp/pettrain_cli.log`
- 活服实证(编辑器 Play):新号推主线到 100190 → 点任务 → 手指指宠物图标/直开剑魄同修页 →
  手指指"一键提升" → 点若干次到 1阶2星 → 服务端自动完成 → 手指指关闭 → 任务栏领奖。

## 已知边界(下一批)

- 等级线(ctype90 页内完成)缺 OutwardLvSystem 子页;翼影24/符相92/神兵41 缺 RoleView 页签宿主;
- 3D 模型展示位(res)归 3D 线;星级满星升阶动画/星光特效未接(特效线);
- 神巫/天妖灵魄页签 disabled(PartnerBaseView/DemonMainView 未移植)。
