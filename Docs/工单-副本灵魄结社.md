# 工单:副本入口(御魂本)+ 灵魄镶嵌 + 结社加入 最小闭环

日期:2026-07-02(第18轮侦察代理产出,主控审定)
目标:解主线卡点 #112(100980 挑战御魂塔1层,ctype9 id=12)+ 101522(通关3层,ctype57)、#113(100990 镶嵌一次御魂,ctype33)、#123(101080 加入结社,ctype14)。

## 共同点(重要)
三系统任务判定都是「服务端状态达成即判定」(副本记录/已穿符文数/guild_id>0),客户端只需把协议打通,
完成态由既有 30001 任务推送自动刷新——不需要客户端自算进度。

## C:结社加入(最简,优先)——协议段 pt_400
- **40004 创建公会**(C2S "ls" CfgId=2, GuildName;回包 error_code:i, guild_id:l)——空服最短路径:
  建社成功 → 服务端 lib_guild_api common_join_guild → join_guild 事件 → 任务完成。**不依赖已有公会**。
- 可选:40001 列表(C2S "shh" name,pageSize,pageNo;回包 page_total:h, page_no:h, guild_list[u16×{guild_id:l,
  guild_name:s, guild_lv:h, guild_exp:i, chief_id:l, chief_name:s, member_num:h, member_capacity:h, is_apply:c,
  auto_approve_power:i, combat_power:l, merge_status:c, is_master:c}])+ 40003 一键申请(无参;回包 error_code:i, guild_id:l, apply_type:c)。
- 老端行为:GuildJoinView 空列表时引导建社;打开面板时若主线==101080 会发 30008(pp_task 补触发 join_guild 判定,照做);
  一键申请成功且主线==101080 自动关窗。DoTask case14 → GuildModel.OpenGuildView()。
- Unity 已有 GuildListView/GuildListFlow(未接协议)。TEMP 壳:结社列表(40001)+[一键申请](40003)+[创建结社](40004,名字输入用固定文本框)。
- ⚠建社消耗档位(CfgId=2 对应 data_guild_m 配置)未确认——失败显码即可,留后。

## B:灵魄镶嵌——协议段 pt_167
- **16700 全量**(打开面板时发,无参;回包 rune_point:i, rune_chip:i, skill_lv:h, rune_list[u16×{pos_id:c, if_open:c,
  goods_id:l, goods_type_id:i, color:c, lv:h, attr_list[u16×{attr_id:i, attr_num:i, awake_lv:i, awake_exp:i,
  next_power:l, cur_power:l}]}], rune_sum_power:l)。
- **16701 镶嵌**(C2S "cl" pos_id, goods_id;回包 code:i, pos_id:c, new_goods_id:l, old_goods_id:l, new_goods_type_id:i)。
- 任务=镶嵌 1 次(孔位1无条件开放);符文物品来自背包(货物 loc=rune_bag,15010 按 pos 请求——pos 值查老端 GOODS_POS_TYPE.rune_bag,BagController 现只请求 bag(4),需加一发或在壳打开时请求)。
- 老端锚点:RuneBagItem.ts:31(发 "cl")、pp_rune.erl:119(镶嵌成功 rune_num 事件)、DoTask case33 → SecretTreasureMainView。
- Unity 已有 Module/Core/Rune 的 View 壳(未接 16700/16701)。TEMP 壳:槽位1状态+符文背包列表(需 15010 rune_bag pos 数据)+[镶嵌]按钮。
- 无强制配表(孔位开启信 16700 的 if_open)。

## A:御魂本副本(最大骨头,拆两票)——协议段 pt_610
### A1 通用副本进出结算壳
- **61001 进入**(C2S "i" dun_id;回包 dun_id:i, scene_id:i, error_code:i, error_code_args:s)
- **61002 退出**(已接在 AutoBrushController,复用常量 DUNGEON_EXIT)
- **61013 结算**(推送:result:c, help_type:c, score:h, pass_time:h, rela_list[...], drop_reward:ObjectList, reward_list:ObjectList
  ——rela_list 具体 schema 落实现时再从 ClientProtocol.json 摘,按序读完)
- **61020 副本状态**(回包 dun_type:c + dun_list[u16×{dun_id:i, daily_count:h, weekly_count:h, permanent_count:h,
  reset_count:h, vip_count:h, add_count:h, is_sweep:c, rec_data[...]}];请求格式查 pt_610 read)
- 任务目标:发 61001(dun_id=12001)进御魂本1层 → 场景内打完(战斗复用现有场景战斗)→ 61013 结算 → 61002 退出。
- 配表 config_dungeon(39 列,列序在 config_table_default;12001~12003:type=12/scene_id=2005/condition erlang term 用已有 ErlangParser)。
- ⚠澄清(修记忆):AutoBrush(133xx)=主线结界守护副本,与御魂本(61xxx)是两套;大妖副本也走 61001 家族。
### A2 御魂本专属 UI(DungeonRuneEnterView 关卡列表/每日/首通)全部留后。

## 接线(主控):Proto 常量、GlobalEvent、ControllerHub、DoTask case14/33/9·57、RenderAll。
## 红线:同前。副本 A1 票需要活服才能真验(进场景),合成包只验读序;诚实标注。
