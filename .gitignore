# Unity generated folders at the project root
/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/
/[Ll]ogs/
/[Uu]ser[Ss]ettings/
/[Mm]emoryCaptures/
/[Rr]ecordings/

# Build output folders at the project root
/[Bb]uild/
/[Bb]uilds/

# IDE and editor local files
/.vs/
/.vscode/
/.idea/
*.csproj
*.sln
*.slnx
*.unityproj
*.user
*.userprefs
*.suo
*.tmp
*.pidb
*.booproj

# OS generated files
.DS_Store
Thumbs.db
Desktop.ini

# Local logs
*.log

# Keep Unity source and resources tracked:
# Assets/, Packages/, ProjectSettings/, *.meta, prefabs, scenes, textures,
# materials, models, audio, shaders, and other resource files are intentionally
# not ignored.

# LayaUI converter reports (generated)
/Reports/

# LayaUI 转换器生成产物。已定稿入库,让协同开发 clone 即可拿到,无需依赖老 Laya 项目再生成:
#  - resource/(UI 图集/贴图/配置)走 LFS(见 .gitattributes Assets/GameRes/resource/**)
#  - Prefabs/UI(已转换的 UI 预制体,文本)已移出 ignore;后续新转换模块按模块提交
/Assets/_App/Scenes/LayaUIPreview.unity*

# 例外(P0 资源可复现门 — 见 Docs/Claude任务包-主线竖切-第1轮.md):
# 进游戏「主竖切」结构闭包已用 `git add -f` 入库,使 Remote_Prefabs.asset 里
# mainuimodule/mapmodule/settingmodule/basewindowskin 等 Addressables key 在
# clean checkout 可解析。闭包 = 这 4 个根 prefab 递归引用到的全部 UI 预制件,
# 共 23 个 prefab(+其 .meta +所在目录 .meta)。它们是 tracked 文件,上面的
# /Assets/Prefabs/UI/ 规则对已跟踪文件不生效;其余 UI prefab 仍忽略(可再生成)。
# 注:这些 prefab 引用的散图/字体仍在被忽略的 /Assets/GameRes/resource/ 下,
# clean checkout 时结构与脚本组件可加载,缺失的静态贴图=优雅降级(动态图运行时走 ResManager)。

# ── 生成物 / 缓存 / 构建产物(不入库,纯浪费空间)────────────────────────
# 以下全部为「未跟踪」的机器本地产物,忽略即可,无需 git rm。

# Android IL2CPP / CMake / Ninja 原生构建临时目录(.o / ninja / CMakeCache…)
/.utmp/

# Addressables 打包输出:bundle + catalog,每次构建重生成,~GB 级
/ServerData/

# Addressables 编辑器生成的机器/CDN 相关缓存(Unity 官方建议忽略:含本机 CCD
# 桶信息,逐人不同,入库会引起无谓冲突)
/Assets/AddressableAssetsData/ProfileDataSourceSettings.asset
/Assets/AddressableAssetsData/ProfileDataSourceSettings.asset.meta
# 注:addressables_content_state.bin(Android/WebGL,各 ~750K)不忽略——体积小,
# 且它是内容热更(Update a Previous Build)的基线,漏记反而会出问题,照常入库。

# PoseProbe 调试渲染帧(写到工程根目录)
/PoseProbe_*.png

# 无头测试 harness 的浏览器 profile/缓存、依赖与截图产物
#(*.cjs / *.mjs 脚本本身保留入库)
/Tools/headless/node_modules/
/Tools/headless/out*/
/Tools/headless/*profile*/

# 本地审计、报表和可搬运交付包（不参与版本同步）
/outputs/

# Python 工具的字节码缓存(Tools/LayaUI 等)
__pycache__/
*.pyc
